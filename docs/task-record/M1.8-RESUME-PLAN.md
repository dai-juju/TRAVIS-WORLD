# M1.8 Resume Plan — /clear 후 새 세션 재개용 완전 가이드

> **작성일**: 2026-05-26 (2026-05-27 갱신 — 8.3b-1 ✅ 완료 / 2026-05-28 갱신 — 종단 게이트 **G1~G5 재서술 + 63셀→13셀 현재 스냅샷** 정합. 전체 63셀 시계열 검증은 8.3c (β) 이월 따라 M1.8.5 `[8-15]` 로 이관)
> **목적**: 사용자가 `/clear` 후 새 세션에서도 M1.8 의 진행 상태 / 잔여 작업 / 사용자 결정 영역 / 다음 명령을 **30초 안에** 회복할 수 있도록 단일 진실 원천 제공.
> **사용 방법**: 새 세션 진입 시 본 파일을 가장 먼저 읽기 → §0 한 줄 요약 + §1 현재 시점 + §3 다음 명령 순서.

---

## 0. 한 줄 요약

> **"M1.8 의 8.3b ✅ + D20/D21/D22 ✅ + 8.3c (β) 이월 확정 (2026-05-27). 본 마일스톤 잔여 = **종단 게이트만** (G1 13셀 현재 스냅샷 + PHAROSUSDT smoke + NULL 비율 ∥ G2 자동게이트 + dry-run 6항목 → G3 0 Critical 자문 → G4 deferred 묘비 일관성 → G5 마일스톤 종료, ~4.5~6h). 전체 63셀 시계열 검증 + 8.3c 전체 (schema + fetcher 6종 + normalize + loop + 실 backfill) = 'M1.8.5 history backfill' 별도 사이클로 이월. 다음 첫 명령 = `@roadmap-milestone-manager` 자문으로 종단 게이트 substep 분해."**

---

## 1. 현재 시점 (2026-05-27)

### 1.1 마지막 commit (본 세션 종료 시점)

```
7a91632 docs(m1.8-step3-c-defer): 8.3c 전체 M1.8.5 별도 사이클 이월 (β 결정) + 종단 게이트 진입 가이드
3396071 docs(m1.8-d20-d22): 사용자 결정 D20=C / D21=B / D22=A 확정 + deferred 묘비 처리
13e22b1 docs(m1.8-step3-b-deploy): Hetzner deploy + dry-run 시뮬레이션 6 항목 100% 일치 검증
58bc9a1 feat(m1.8-step3-b-code): worker bootstrap historyBackfillTask 등록 + dry-run 안전 우회
9771954 docs(m1.8-resume-plan): /clear 후 세션 재개용 단일 진실 원천 신설 + 3 docs cross-link
```

### 1.2 M1.8 누적 (commit 22+)

| # | Substep | 상태 | 핵심 commit |
|---|---|---|---|
| 1 | 8.0 사전 진단 + 자문 + D5~D14 | ✅ | — (task-record 만) |
| 2 | 8.1 Schema migration | ✅ | `3b2e0f8` (task-record 신설) |
| 3 | 8.2a-1 worker 컬럼명 hotfix | ✅ | 누적 commit 8건의 일부 |
| 4 | 8.2a-2 4 fetcher + fundingInfoTask + 2 hotfix | ✅ | 누적 |
| 5 | 8.4 SPOT cleanup + 8.4-d hotfix | ✅ | `a0831be` / `a750704` |
| 6 | 8.5-a/b/c/d marketUnits.ts + canonical-metrics.md | ✅ | `c11c335` / `e4e8082` / `b79d735` |
| 7 | **8.3a** historyBackfillTask + dry-run mode | ✅ | `5123780` / `77c1c27` |
| 8a | **8.3b-1** worker bootstrap 등록 + barrel re-export + 검증 (dryRun:true 안전 우회) | ✅ | `58bc9a1` |
| 8b | **8.3b-2** Hetzner deploy + journal 시뮬레이션 6 항목 100% 일치 검증 | ✅ | `13e22b1` |
| 8c | **D20/D21/D22** 사용자 결정 (C/B/A 모두 권장안 채택) + deferred 묘비 | ✅ | `3396071` |
| 8d | **8.3c (β) 이월 확정** + 종단 게이트 진입 가이드 + docs sync | ✅ | `7a91632` |
| 9 | **8.3c** 전체 (schema migration + fetcher 6종 + normalize + loop + 실 backfill) | ⏭️ **M1.8.5 별도 사이클 이월** (β 결정) | — |
| 10 | **종단 게이트** G1 13셀 현재 스냅샷 + PHAROSUSDT smoke + NULL 비율 ∥ G2 자동게이트 + dry-run 6항목 → G3 0 Critical → G4 deferred 묘비 → G5 M1.8-complete.md | 🟡 **다음 (~4.5~6h)** | — |
| 11 | **M1.8.5 history backfill** (별도 사이클, M1.8 종단 후) | ⏳ M1.8 종료 후 | — |

### 1.3 산출물 위치 (8.3b-2 까지)

- **worker 코드 (M1.8 신설/변경)**:
  - `apps/worker/src/poller/tasks/historyBackfillTask.ts` (~200줄, dry-run mode 만, 실 호출 X) — 8.3a 신설
  - `apps/worker/src/poller/tasks/index.ts` (barrel) — 5 task re-export (8.3b-1)
  - `apps/worker/src/index.ts` — 5 task bootstrap (perSymbol/ticker24hr/fundingInfo/premiumIndex/historyBackfill, 8.3b-1)
- **task-record**: `docs/task-record/M1.8-step3-history-backfill.md` (~310줄, §5.1~5.5 + §7 사용자 결정 묘비)
- **canonical docs**: `docs/canonical-metrics.md` (8.5-c 신설, ~500줄, 9 섹션)
- **format 헬퍼**: `apps/web/lib/format/marketUnits.ts` (8 함수, 8.5-a)
- **Hetzner 운영 상태**: `/opt/travis` 의 worker active running since 2026-05-27 06:47:47 UTC (5 task healthy, dry-run 가동 중)

---

## 2. 남은 작업 분해

### 2.1 Sub-substep 8.3b ✅ 전체 완료 (2026-05-27, ~50분 실제)

**범위**:
1. ✅ **8.3b-1 (코드, ~20분)**: barrel re-export + `apps/worker/src/index.ts` 에 `createHistoryBackfillTask({ ..., dryRun: true })` 등록 + type-check + lint + commit (`58bc9a1`)
2. ✅ **8.3b-2 (운영, ~30분)**: git push + Hetzner git pull (12 commits Fast-forward) + systemctl restart + journal 6분 관찰 + 시뮬레이션 출력 6 항목 100% 일치 검증

**검증 결과**:
- 시뮬레이션 2 cycle 출력 (06:47:49 부팅 즉시 + 06:52:49 INTERVAL_MS 후) 모두 정확.
- 608 symbols / 27,360 REST / 20,471,360 row / 2.28h / 1.91GB / 10MB — RESUME-PLAN §2.1 예측치 100% 일치.
- 다른 4 task 영향 0건 + dry-run 가드 정상 (실 호출 0건).
- Hetzner 운영 healthy (Memory 85.9M / NRestarts 0 / Active running).

**예상 1차 시뮬레이션 출력** (Hetzner 가동 시):
```
[historyBackfillTask:dry-run] 시뮬레이션 완료 (~50 ms):
  - TRADING USDM 심볼 수: 608개 (lookback=14일)
  - 호출 수 (1회 backfill): 27,360 REST (= 608 × 5 × 9)
  - Row 수 (1회 backfill): 20,478,720 row (= 608 × 5 × 6,734)
  - 시간 예상: 2.28 h (8,208 초, 200 req/min)
  - Supabase 용량: 1.91 GB (1,953 MB, 20,478,720 × ~100 byte)
  - Worker 메모리 footprint: 약 10 MB
```

### 2.2 ~~사용자 결정 영역 (D20/D21/D22)~~ — ✅ **2026-05-27 전체 확정 (C/B/A 채택)**

> **이전 내용**: 8.3b 완료 후 8.3c 진입 전 사용자가 결정해야 할 3 영역 (D20 실 backfill 시점 / D21 Basis 포함 / D22 schema interval 컬럼).
>
> **결정 결과** (2026-05-27): D20=C / D21=B / D22=A 모두 권장안 채택 — 자세한 근거 + 묘비는 **§5 사용자 결정 영역** 참조. deferred `[8-12]/[8-13]/[8-14]` 묘비 처리 완료 (commit `3396071`).

### 2.3 ~~Sub-substep 8.3c — 실 backfill~~ — ⏭️ **2026-05-27 M1.8.5 별도 사이클로 이월 (β 결정)**

> **이전 가이드**: 사용자 D20/D21/D22 결정 후 본 마일스톤 안에서 8.3c 코드 + 실 backfill 진행 예상.
>
> **변경** (2026-05-27 (β) 결정, commit `7a91632`): 8.3c 전체 (D22 schema migration + BinanceUsdmAdapter history fetcher 6종 [D21=B] + normalize 6종 + dryRun=false loop + 실 backfill ~2.97h ~25M row) 가 **"M1.8.5 history backfill"** 별도 사이클로 이월. 본 마일스톤은 종단 게이트 (§2.4) 로 직행.
>
> **M1.8.5 진입 시 산정 분량**: 4~6h 코드 + ~10분 schema migration + ~2.97h 실 backfill = 총 ~7~9h. 단일 진실 원천: deferred `[8-15]` + `docs/task-record/M1.8-step3-history-backfill.md §5.4`.

### 2.4 M1.8 종단 게이트 — G1~G5 (2026-05-28 재서술, ROADMAP §완료 기준과 단일 정합)

> **재서술 배경**: 8.3c (β) M1.8.5 이월로 `history_futures_indicator` 9-인터벌 데이터가 M1.8 시점 미존재 → "63 셀 (7 metric × 9 interval 시계열)" 검증 불가. 따라서 종단 게이트는 **G1 13셀 현재 스냅샷** (모두 `now_futures_indicator` + `symbols` 현재값) 으로 축소. 전체 63셀 시계열 검증은 M1.8.5 (`[8-15]`) 로 이관.

**G1 — 수동 검증 (현재 스냅샷)**:
- **13 셀 사이트=DB 일치** — Binance USDM BTCUSDT `canonical-metrics.md §5` 13 행 수동 검증 (task-record 스크린샷 첨부). 허용 오차: Mark/Index ±tickSize · Predicted Funding ±1bp(동시 캡처) · Countdown ±2초 · Basis ±tickSize/±0.005%p · 나머지 정확 일치.
- **PHAROSUSDT 4h funding smoke** — `fundingInfo` reload 후 `symbols.funding_interval_hours = 4` 확인 (미등재 시 syncSymbolsTask 24h reload 후 회수). canonical-metrics.md §6 매트릭스.
- **NULL 비율** — `now_futures_indicator` 의 `top_ls_ratio_positions` / `global_ls_ratio` / `basis` / `basis_rate` 모두 < 20% (TRADING) + `now_spot_ticker` < 5% (8.4 cleanup 후).

**G2 — 자동 게이트**:
- `pnpm rls-check` / `type-check` / `lint` / `test` PASS.
- **dry-run 시뮬레이션 6 항목 100% 일치** (8.3b-2 ✅ 검증 완료 — 608 심볼 / 27,360 REST / 20.5M row / 2.28h / 1.91GB / 10MB). 실 backfill `rows > 50K` 검증은 M1.8.5 (`[8-15]`) 이관.

**G3 — 0 Critical 자문**:
- `@code-reviewer` 0 Critical / `@security-auditor` 0 Critical / `@crypto-trader` advisory (단위 misread 우려 0).
- `@crypto-domain-expert` canonical-metrics.md 검증 ✅.

**G4 — deferred 묘비 일관성 (신규 회수 0건)**:
- `[3-43]`/`[3-48]`/`[3.5-7]`/`[3-53]`/`[3-54]`/`[3-55]` 이미 §8.4/§8.5 묘비 완료 + `[8-12~14]` D20-D22 묘비 완료 확인 + `[3-50]`(M2+ 보존)/`[3-62]`(route.ts 분할, M1.8 범위 밖) **미회수 명시** + `[8-15]` 이월 entry 확인.

**G5 — 마일스톤 종료**:
- ROADMAP §M1.8 ✅ 마커 + PRD §6 + M2-plan §M1.8 흡수 이력 + `docs/task-record/M1.8-complete.md` 신설 + memory `project_m1_8_complete.md` 전환 + 7 docs 일관성.

---

## 3. 새 세션 진입 시 첫 5 분 가이드

### 3.1 첫 명령 (종단 게이트 진입)

```powershell
# 1) 현재 상태 확인 — 마지막 commit 가 (β) 이월 묘비 처리 commit 이어야 함
git log --oneline -5
# 기대 출력: (이월 묘비 commit) / 3396071 / 13e22b1 / 58bc9a1 / 9771954

# 2) 본 plan 파일 위치 확인 + 종단 게이트 분해
Get-ChildItem docs/task-record/M1.8-RESUME-PLAN.md

# 3) @roadmap-milestone-manager 자문 (종단 게이트 substep 분해)
#    Claude 가 자동 호출하거나 사용자가 명시: "M1.8 종단 게이트 시작. @roadmap-milestone-manager"
```

**종단 게이트 분해 (G1~G5 — §2.4 단일 정합, 2026-05-28 재서술)**:
1. **G1** 13셀 현재 스냅샷 수동 검증 (canonical §5) + PHAROSUSDT 4h smoke + NULL 비율 — task-record 스크린샷 첨부
2. **G2** `pnpm rls-check`/`type-check`/`lint`/`test` PASS + dry-run 6 항목 (8.3b-2 ✅ 재확인)
3. **G3** `@code-reviewer` + `@security-auditor` + `@crypto-trader` 0 Critical 확인
4. **G4** deferred 묘비 일관성 검증 — **신규 회수 0건** (이미 §8.4/§8.5/D20-D22 묘비 완료, `[3-50]`/`[3-62]` 미회수 명시, `[8-15]` 이월 확인)
5. **G5** ROADMAP §M1.8 ✅ 종료 + PRD §6 + M2-plan §M1.8 흡수 이력 + `docs/task-record/M1.8-complete.md` 신설 + memory `project_m1_8_in_progress.md` → `project_m1_8_complete.md` 전환

### 3.2 8.3b-1 의 Edit 위치 (이미 완료 — 2026-05-27)

**완료된 변경**:
- `apps/worker/src/poller/tasks/index.ts` (barrel) — `createHistoryBackfillTask` + `HistoryBackfillTaskDeps` re-export 추가 + 헤더 주석 "5 task 등록" 으로 갱신.
- `apps/worker/src/index.ts`:
  - import: `createHistoryBackfillTask` (알파벳 순)
  - poller.register: 신규 등록 (premiumIndexTask 다음, StreamRouter 등록 전, `dryRun: true` 안전 우회)
  - 주석: "M1.8 §8.3b-1 (2026-05-27) — 5 task 등록" + 등록 순서 의미 갱신

### 3.3 8.3b-2 의 Hetzner deploy 절차

```powershell
# SSH 키 위치
$KEY = "C:\Users\samsung\.ssh\travis_hetzner"
$HOST = "travis@178.105.38.94"

# Hetzner 의 sudo password 는 별도 채널로 공유 (commit/docs 에 절대 기록 X — 2026-05-27 변경).
# - SSH non-interactive 세션 의 sudo 는 tty_tickets 함정 — 각 sudo 마다 -S 로 stdin 전달 필요.
# - 안전 패턴: printf '%s\n%s\n' 'PW' 'PW' | ssh ... "sudo -S -p '' CMD1 && sudo -S -p '' CMD2"

# 1) git pull (2026-05-27 경로 정정: /home/travis/TRAVIS-world ❌ → /opt/travis ✅)
ssh -i $KEY $HOST "cd /opt/travis && git pull"

# 2) (조건부) pnpm install — lockfile 변경 시에만. 변경 없으면 스킵 가능.
#    `git pull` 출력의 file list 에 pnpm-lock.yaml / package.json 등장 시에만 실행.
ssh -i $KEY $HOST "cd /opt/travis && CI=true pnpm install --frozen-lockfile"

# 3) build 단계 = 불필요 (worker 는 tsx 로 .ts 직접 실행 중 — 2026-05-27 발견).
#    `ps aux | grep tsx` 로 확인. systemd ExecStart 가 tsx src/index.ts 이면 build 스킵.

# 4) worker 재시작 (sudo + -S + stdin password)
printf '%s\n' '<sudo-password>' | ssh -i $KEY $HOST "sudo -S -p '' systemctl restart travis-worker"

# 5) journal 6분 관찰 (INTERVAL_MS=5분 대비 안전 마진)
printf '%s\n' '<sudo-password>' | ssh -i $KEY $HOST "sleep 330 && sudo -S -p '' journalctl -u travis-worker --no-pager --since '6 minutes ago' | grep -iE 'historyBackfill|dry-run|시뮬레이션' | tail -80"
# → [historyBackfillTask:dry-run] 시뮬레이션 완료 로그 확인
```

### 3.4 ~~검증 후 사용자 결정 (D20/D21/D22)~~ — ✅ **2026-05-27 확정 완료**

> 사용자 결정 = D20=C / D21=B / D22=A (권장안 100% 채택). 8.3c (β) 이월 결정으로 본 마일스톤 안에서는 추가 진입 없음. **본 §3.3 의 Hetzner deploy 절차는 M1.8.5 진입 시점에 재활용 가능** — 그 시점에 schema migration 선행 + worker 코드 신설 + restart + journal 검증 동일 패턴.

---

## 4. 영구 규율 재확인 (2026-05-26 사용자 명시)

> **"각 서브스텝을 진행하면서 진행사항을 docs/task-record/ 와 docs/deferred-task.md 에 기록해야한다는 점을 잊으시면 안됩니다. 앞으로도 적절한 문서에 기록하면서 진행해야하는 점을 잊으시면 안됩니다."**

**8.3b/8.3c/종단 게이트 진행 시 각 sub-substep 마다 의무**:

1. `docs/task-record/M1.8-step3-history-backfill.md` §5 즉시 보강
2. `docs/deferred-task.md` 회수/신설 즉시 등재
3. `docs/ROADMAP.md` §M1.8 마커 즉시 갱신
4. commit message 에 substep 명시

**관련 memory**:
- `feedback_docs_record_per_substep.md` (영구 규율 2)
- `feedback_ask_before_assume.md` (영구 규율 1)

---

## 5. 사용자 결정 영역 ✅ 전체 확정 (2026-05-27)

### 5.1 ~~D20 — 실 backfill 시점~~ — ✅ **(C) 마일스톤 종료 후 별도 사이클 채택**

> 사용자 컨펌 (2026-05-27): 본 마일스톤의 본질 (현재 시점 카탈로그 완성) 과 시계열 backfill 가치가 별개라는 판단에 동의. 종단 게이트 통과 후 "M1.8.5 history backfill" 또는 M2 Step 0 영역으로 분리하여 운영 비용 (~2.28h cycle + ~2.5GB Supabase) 을 별도 의사결정으로 격리.

### 5.2 ~~D21 — Basis 포함 여부~~ — ✅ **(B) 6 metric 채택**

> 사용자 컨펌 (2026-05-27): OI / Top LSR Acc / Top LSR Pos / Global LSR / Taker / **Basis** 6 metric. 사용자 요구 #3 "선물 지표 7+" 의 시계열 coverage 정합 + canonical-metrics.md §2 의 Basis 정의 즉시 활용. 분량 +30% (27K→33K REST, 20.5M→25M row, 2.28h→2.97h, 1.91GB→2.5GB) 는 IP quota / Supabase 용량 안 안전 margin.

### 5.3 ~~D22 — schema interval 컬럼~~ — ✅ **(A) interval VARCHAR(5) ADD + PK 재구성 채택**

> 사용자 컨펌 (2026-05-27, 비전공자 친화 ASCII 비교 후): 표준 OLAP fact table 패턴 + AI/SQL 쿼리 표준. **schema migration 실제 실행은 8.3c 진입 시점** (한 substep 안에서 migration + fetcher + normalize + loop 묶음). 실 SQL:
> ```sql
> ALTER TABLE history_futures_indicator
>   ADD COLUMN interval VARCHAR(5) NOT NULL DEFAULT '1d';
> CREATE UNIQUE INDEX history_futures_indicator_natural_pk
>   ON history_futures_indicator (exchange, market_type, symbol, interval, recorded_at);
> ALTER TABLE history_futures_indicator
>   ALTER COLUMN interval DROP DEFAULT;
> ```

### 5.4 결정 결과 종합 — ✅ **(β) 채택** (2026-05-27)

사용자 결정 (2026-05-27): **(β) 8.3c 전체 이월** — 본 마일스톤 = 종단 게이트로 직행, 8.3c 전체 (schema migration + fetcher 6종 + normalize + loop + 실 backfill) = "M1.8.5 history backfill" 별도 사이클.

**결정 근거**:
- D20=C 의 본질 "운영 분리 명확" 을 코드 신설까지 자연 확장 = cleaner.
- 본 마일스톤의 "현재 시점 카탈로그 완성 + 표시 단위 정공 + 사이트=DB 일치" 본질 100% 유지.
- 본 마일스톤 종단 게이트 검증 단순 (13 셀 현재 스냅샷 — 63 셀 시계열은 M1.8.5 이관) + scope 깔끔.
- CLAUDE.md "한 번에 하나의 작업" + "scope 넓히지 마" 원칙 정합.

**다음 사이클 진입 가이드**:
- 종단 게이트 통과 후 M1.8 ✅ 종료 선언.
- M1.8.5 (또는 M2 Step 0) 에서 schema migration + fetcher 6종 + normalize + loop 한 묶음 진행 (예상 4~6h 코드 + 2.97h 실 backfill).
- historyBackfillTask 는 본 마일스톤 중 dry-run 5번째 task 영구 가동 — M1.8.5 진입 시 `dryRun:false` 1줄 변경 + schema migration 적용 후 즉시 실 backfill 가능.

---

## 6. 본 plan 파일의 영구 가치

1. **세션 재개 비용 최소화**: `/clear` 후 30초 이내 맥락 회복.
2. **사용자 결정 영역 영구화**: D20/D21/D22 가 deferred 와 본 plan 양쪽에 기록 → 어느 세션이든 동일 컨텍스트.
3. **명령 예시 직접 제공**: PowerShell + Edit 위치 + Bash 까지 명시 → 새 세션이 추측 없이 진입.
4. **마일스톤 종료 후에도 가치**: 본 plan 의 §5 (사용자 결정 영역) + §3.3 (Hetzner deploy 절차) 가 M2 의 새 task 진입 시점에도 reference.

---

## 7. 관련 docs 일관성 보장 매트릭스 (2026-05-27 갱신)

본 plan 파일과 동일 정보가 다른 docs 에 반영되었는지 추적:

| 정보 | M1.8-RESUME-PLAN.md | M1.8-step3-history-backfill.md | deferred-task.md | ROADMAP.md | M2-plan.md | PRD.md | Architecture.md |
|---|---|---|---|---|---|---|---|
| 8.3a ✅ 완료 | §1.2 표 #7 | §5.1 | — | §M1.8 8.3a 행 | §M1.8 cross-link | §6 (M1.8 줄) | — |
| **8.3b ✅ 완료** (2026-05-27) | §1.2 #8a/8b + §2.1 | §5.2 + §5.3 | — | §M1.8 8.3b-1/8.3b-2 행 | "현재 위치" 줄 | §6 (M1.8 줄) | — |
| **D20/D21/D22 ✅ 확정** (2026-05-27) | §1.2 #8c + §2.2 묘비 + §5 | §7 묘비 | `[8-12]`/`[8-13]`/`[8-14]` 묘비 | (cross-link in 8.3b/c 행) | — | — | — |
| **8.3c (β) M1.8.5 이월** (2026-05-27) | §1.2 #8d/#9 + §2.3 묘비 + §5.4 | §5.4 묘비 + 8 단계 분해 | `[8-15]` 신규 등재 | §M1.8 8.3c 행 (이월 마커) | Context 2-b 신규 | §6 (M1.8.5 cross-link 가능) | — |
| 종단 게이트 잔여 | §1.2 #10 + §2.4 + §3.1 | — | — | §M1.8 종단 게이트 행 | §종단 검증 | §6 | — |
| M1.8.5 history backfill (신규 사이클) | §1.2 #11 | §5.4 분해 | `[8-15]` | §M1.8 8.3c 이월 마커 | Context 2-b | — | — |
| canonical-metrics.md (M1.8 §8.5) | — | (cross-link) | — | §M1.8 §8.5 행 | — | §1 데이터 위생 #9 | "사이트=DB 진실 일치" 영역 |
| marketUnits.ts 8 헬퍼 (M1.8 §8.5) | — | (cross-link) | — | §M1.8 §8.5 행 | — | — | (M1.8 cross-link 가능) |
| Hetzner 운영 발견 (`/opt/travis` + tsx + sudo tty_tickets) | §1.3 + §3.3 | §5.3 | — | — | — | — | — |
| 새 세션 진입 가이드 | §3 | — | — | — | — | — | — |
| 영구 규율 재확인 | §4 | — | — | — | — | — | — |

**원칙**: 본 plan 파일이 단일 진실 원천. 다른 docs 는 cross-link 만 유지 (정보 중복 시 본 파일이 우선).

**미반영 cross-link 알림 (2026-05-27 sweep 결과)**:
- PRD §6 의 M1.8 줄은 본 sweep 에서 신규 추가 진행.
- Architecture 의 marketUnits.ts cross-link 는 M1.8.5 진입 시점에 검토 (현재는 canonical-metrics.md 만 명시 — 충분).

---

## 8. 본 plan 의 종료 조건

- M1.8 종단 게이트 ✅ 통과 시
- 본 plan 파일은 historical reference 로 보존 (삭제 X)
- 다음 마일스톤 (M2 또는 M1.8.5) 의 RESUME-PLAN 신설 시 본 파일 패턴 미러링
