# M1.8 §8.4 — SPOT stale row cleanup + ticker24hrBatchTask TRADING filter

> **상태**: ✅ **완전 종료 (2026-05-26)** — 8.4-a + 8.4-b + 8.4-c + 8.4-d hotfix 모두 적용.
> **선행**: M1.8 §8.2a ✅ 완료 (2026-05-26)
> **commit 누적 (10건)**:
>   - `7f50eef feat(m1.8-step4-a): ticker24hrBatchTask TRADING allowlist filter`
>   - `a750704 fix(m1.8-step4-d): markPriceWsHandler + premiumIndexTask TRADING allowlist`
>   - (본 응답) `docs(m1.8-step4-deploy): ✅ 종료 + [3-53] 회수`
> **단일 진실 원천**: 본 task-record + `docs/task-record/M1.8-step0-pre-infra.md` §2 Fact 2

---

## 1. 배경 — SPOT 60% NULL 의 정체

M1.8 §8.0 진단 결과: `now_spot_ticker` 3593 rows 중 1419 만 채워짐 (40%) — 나머지 60% (2185 rows) 가 stale NULL. M2-plan §Step 0 의 5대 사실 Fact 2.

### 진짜 원인 (8.4 진입 후 코드 진단):

| 코드 경로 | TRADING 필터 적용? | 영향 |
|---|---|---|
| `tickerWsHandler.ts` (WS) | ✅ 적용 (M1.4 Step 4.7) | BREAK 심볼 last_price/volume push 차단 |
| `ticker24hrBatchTask.ts` pollSpot (REST) | ❌ **미적용** | BREAK 심볼 row INSERT + 24h 6 필드만 채움 |

**메커니즘**:
- `fetchTicker24hr` 가 SPOT 전체 ticker (TRADING + BREAK 등) 반환
- `extractSpot24hrFieldsOnly` 가 PK 3 + 24h 변화율 6 필드만 추출
- `upsertNowSpotTickerPartial` 가 ON CONFLICT (PK) 없으면 INSERT
- → BREAK 심볼 row 가 INSERT 되면서 PK + 24h 6 필드만 채움
- → `last_price` / `volume` / `high_price` / `low_price` 등 (WS 만 채우는 컬럼) 은 NULL
- → row 만 존재 + 60% NULL stale state

---

## 2. 해결책 — 두 단계 동시 적용

### Phase 1 (코드 fix) ✅ 완료 (2026-05-26)

`ticker24hrBatchTask.ts` 에 TRADING allowlist 적용:
- `Ticker24hrBatchTaskDeps` 에 `tradingSymbolsByMarket?: Record<MarketType, Set<string>>` 옵션 인자 추가 (tickerWsHandler 패턴 미러링)
- `pollSpot` / `pollOneFutures` 가 fetchTicker24hr 응답 후 `.filter((row) => !allow || allow.has(row.symbol))` 적용
- `apps/worker/src/index.ts` 의 `createTicker24hrBatchTask` 호출에 `tradingSymbolsByMarket` 주입

**검증**: type-check 4/4 PASS / lint 4/4 PASS.

### Phase 2 (DB cleanup) — 사용자 Supabase Studio SQL Editor 실행 예정

옛 stale row 일회성 DELETE. SQL:

```sql
-- M1.8 §8.4 — SPOT/Futures stale row cleanup (2026-05-26)
-- now_*_ticker / now_futures_indicator 에서 symbols 의 TRADING 상태가 아닌 row 일괄 삭제.

DELETE FROM now_spot_ticker
WHERE NOT EXISTS (
  SELECT 1 FROM symbols s
  WHERE s.exchange = now_spot_ticker.exchange
    AND s.market_type = now_spot_ticker.market_type
    AND s.symbol = now_spot_ticker.symbol
    AND s.status = 'TRADING'
);

DELETE FROM now_futures_ticker
WHERE NOT EXISTS (
  SELECT 1 FROM symbols s
  WHERE s.exchange = now_futures_ticker.exchange
    AND s.market_type = now_futures_ticker.market_type
    AND s.symbol = now_futures_ticker.symbol
    AND s.status = 'TRADING'
);

DELETE FROM now_futures_indicator
WHERE NOT EXISTS (
  SELECT 1 FROM symbols s
  WHERE s.exchange = now_futures_indicator.exchange
    AND s.market_type = now_futures_indicator.market_type
    AND s.symbol = now_futures_indicator.symbol
    AND s.status = 'TRADING'
);

-- 검증
SELECT 'now_spot_ticker' AS tbl, COUNT(*) AS total FROM now_spot_ticker
UNION ALL SELECT 'now_futures_ticker', COUNT(*) FROM now_futures_ticker
UNION ALL SELECT 'now_futures_indicator', COUNT(*) FROM now_futures_indicator;
```

**기대 결과**:
- `now_spot_ticker` 3593 → ~1408 (SPOT TRADING 만)
- `now_futures_ticker` 746 → ~608 (USDM TRADING + COINM 30)
- `now_futures_indicator` 746 → ~636 (USDM 608 + COINM 28)

### Phase 3 — Hetzner deploy + 검증

deploy 후:
- ticker24hrBatchTask 가 TRADING 만 upsert
- 향후 BREAK 심볼 추가 INSERT 차단
- NULL 비율 60% → < 5% (가능한 최대 = WS 가 아직 push 안 한 일부)

---

## 3. 데이터 위생 9 원칙 체크

| # | 원칙 | 본 작업 적용 |
|---|---|---|
| 1 | Instrument lifecycle status | ✅ symbols.status = 'TRADING' allowlist 일관 |
| 2 | REST + WS allowlist 필터 | ✅ ticker24hrBatchTask REST 측 fix (WS 는 기존 적용) |
| 3 | 24h 주기 재로드 | 변경 없음 (symbols 재로드 24h) |
| 4 | **stale row 정리 + 감지** | ✅ **본 substep 의 핵심 — DB DELETE + 향후 차단** |
| 5 | 극단값 sanity guard | N/A (cleanup 영역) |
| 6 | 워밍업 가드 | N/A |
| 7 | RLS 사전 점검 | 변경 없음 (now_* RLS 그대로) |
| 8 | 공식 docs URL + 조회일자 | 코드 주석 유지 |
| 9 | 사이트 = DB 일치 | ✅ BREAK 심볼이 사용자 카드에서 사라짐 |

---

## 4. 비전공자 한 줄 설명

> **"식자재 창고 (DB) 의 빈 진열대 (BREAK 심볼 row) 60% 정리 + 향후 빈 진열대 안 생기게 요리사 (worker) 의 식자재 분류 도구 강화. 사용자가 SPOT 코인 검색 시 더 이상 '안 거래되는데 보이는' 코인 없음."**

---

## 5. 진행 결과 (2026-05-26)

### 5.1 8.4-a 코드 fix ✅ (commit 7f50eef)
- ticker24hrBatchTask 에 tradingSymbolsByMarket allowlist 적용
- type-check + lint 4/4 PASS

### 5.2 8.4-b DB DELETE ✅ (사용자 Supabase Studio SQL Editor)
- now_spot_ticker: 3593 → **1408** (-2185, SPOT TRADING 정확)
- now_futures_ticker: 746 → **638** (USDM 608 + COINM 30)
- now_futures_indicator: 746 → 638 (다만 8.4-d hotfix 적용 전에 cleanup 했고 deploy 직후 다시 +108 누적 발견)

### 5.3 8.4-c Hetzner 4차 deploy ✅ (사용자 SSH, 새 PID 231737)
- git pull 7e682b0..7f50eef + build + restart 정상

### 5.4 5.4-d Hotfix 발견 + 적용 ✅ (commit a750704)
**검증 결과 진단**: now_futures_indicator 가 cleanup 638 → 다시 746 (+108 BREAK row 누적). 8.4-a 와 동일 함정 패턴이 2 곳 더 발견:
- `markPriceWsHandler.ts` (WS @markPrice@arr@1s): TRADING filter 미적용 → BREAK 심볼 markPrice push 도 indicator partial INSERT
- `premiumIndexTask.ts` (REST 30분, M1.8 §8.2a-2 신설): fetchPremiumIndex 가 전체 USDM (TRADING + non-TRADING) 응답 → 모두 upsert

**적용**:
- markPriceWsHandler Deps 에 tradingSymbolsByMarket 옵션 인자 + handle 에서 filter (tickerWsHandler 패턴 미러링)
- premiumIndexTask 동일 패턴 + 완료 로그에 skip 카운트 노출
- index.ts: createMarkPriceWsHandler + createPremiumIndexTask 호출에 tradingSymbolsByMarket 주입

### 5.5 8.4-c Hetzner 5차 deploy + indicator 재 DELETE ✅ (사용자, 새 PID 232020)
- 3 테이블 모두 최종 검증 통과:
  - now_spot_ticker: 1408 (100% 채움)
  - now_futures_ticker: USDM 608 + COINM 30 (100% 채움)
  - now_futures_indicator: USDM 608 (predicted 100% / last_settled 100% / top_pos 99.7% / basis 94.7%) + COINM 30
- 5분 후 재검증: 608 유지 (BREAK 재INSERT 0건 — 8.4-d hotfix 효과 입증)

## 6. 시스템 전체 정합 효과

**M1.8 §8.4 의 진짜 가치 — CLAUDE.md §위생 #1+#2 시스템 전체 정합 완성**:

| 영역 | 적용 시점 | 마일스톤 |
|---|---|---|
| `tickerWsHandler.ts` (WS @ticker) | 2026-04-22 | M1.4 Step 4.7 |
| `perSymbolTask.ts` (symbol cache) | 2026-04-19 | M1.3 Step 4 |
| **`ticker24hrBatchTask.ts` (REST 1분)** | 2026-05-26 | **M1.8 §8.4-a** |
| **`markPriceWsHandler.ts` (WS @markPrice)** | 2026-05-26 | **M1.8 §8.4-d** |
| **`premiumIndexTask.ts` (REST 30분)** | 2026-05-26 | **M1.8 §8.4-d** |

→ REST + WS 양쪽의 모든 upsert 경로가 TRADING 만 처리. BREAK / SETTLING / CLOSE 심볼이 어디서도 INSERT 안 됨. 향후 stale row 재발 0건.

**부수 효과**:
- BREAK 138 심볼 차단 → worker IP weight 18% 절약
- Supabase write quota 절약 (138 row × 분당 × 24h = 약 200K write 절약/일)
- AI 쿼리 결과 정합성 ↑ (사용자 카드에 "안 거래되는 코인" 보이지 않음)

## 7. Deferred 회수

- `[3-53]` SPOT upsert deadlock 관찰 → ✅ 본 substep 가 직접 해결 (Supabase deadlock 자체는 retryOnTransient 가 흡수, 본질 원인 = BREAK row 누적은 본 substep 가 해결)

## 8. 다음 단계

- 8.5 (표시 단위 정공 + canonical-metrics.md) — 권장 (사용자 카드 즉시 영향)
- 또는 8.3a/b (history backfill) — 큰 작업, 시계열 분석 영역
