# M1.9 Step 3 — history forward-fill 순차 롤아웃 + 검증

> **상태**: 🟡 **진행 중 (2026-06-04)**. Phase A(준비) ✅ + Phase B 일부(USDM 배포 + 라이브 INSERT 실증) ✅ + 라이브 실측 이슈 3건 규명·즉효 fix ✅. **24~48h 안정성·site=DB 정밀 대조·COINM 롤아웃·근본 fix·G1~G5 종단 게이트는 후속 세션**(벽시계 의존).
> **선행**: M1.9 Step 2 ✅ (2026-06-04 코드 완성). 본 Step = 2번째 Hetzner 서버(별도 IP) 라이브 가동.
> **단일 진실 원천**: 본 파일 + `docs/ROADMAP.md §M1.9 Step 3`.

---

## 0. 한 줄 요약

> **"2번째 Hetzner 서버(49.13.138.121, Falkenstein, 별도 IP)에 USDM-only forward-fill collector를 배포. 라이브 가동에서 history가 05-31 정지점→06-04 현재까지 실제로 채워짐(G1 실증). 단 가동 과정에서 3건의 실측 이슈(freshness 25초 timeout / basis -1003 ban / shutdown SIGKILL)를 적발·규명하고, freshness는 인덱스로 즉시 해소, ban·shutdown은 즉효 fix 3종으로 피크 완화. 근본 fix(shared rate limiter + AbortSignal)는 `[8-31]`로 다음 세션 분리."**

## 0.1 서버 정보

| | 2번째 서버 (collector) | 1번째 서버 (production worker) |
|---|---|---|
| 호스트 | travis-collector-history | travis-worker-01 |
| Location | Falkenstein | Nuremberg |
| IP | **49.13.138.121** | 178.105.38.94 |
| 역할 | history forward-fill (USDM) | 실시간 WS relay + now_* upsert |

→ **별도 IP 확정** = `/futures/data` IP quota 충돌 없음 (Step 3 핵심 전제 충족).

---

## Phase A — 배포 준비 (Claude 단독) ✅

### 3-A — 배포 자산 검증 + runbook (`@backend-infra-specialist`)
- deploy 자산 3종 교차 검증: `travis-collector-history.service` / `collector.env.example` / worker `bootstrap.sh`·`runtime-setup.sh` 재사용 가능 확인.
- worker↔collector 차이 4건 반영: ① `collector.env`(worker.env 아님) ② 별도 systemd unit ③ 별도 IP 검증 ④ SSH 락아웃 방지.
- tsx hoist 경로 실존 확인(`/opt/travis/apps/collector-history/node_modules/.bin/tsx`), service_role 키 = worker 동일 값 OK.
- **산출**: `apps/collector-history/deploy/DEPLOY-RUNBOOK.md` (raw Ubuntu 24.04 → 가동 7단계 + Step 0 IP검증 + G1 SQL + 롤백).

### 3-B — 검증 SQL + site=DB 프로토콜 골격
- G1 SQL(신규 INSERT 출현 + 멱등 중복 0) DEPLOY-RUNBOOK 동봉.
- site=DB 대조 프로토콜 골격(BTC/ETH × 5m~1d, COINM OI=contract 단위) — **정밀 실행은 후속 세션**.

---

## Phase B — USDM 배포 (사용자 SSH 실행 + Claude 실증) ✅ (부분)

### 배포 7단계 (사용자 SSH, 2026-06-04)
Step 0(IP 대조 — 두 IP 상이 ✅) → ① bootstrap(8/8 ✅ + travis 접속 검증) → ② runtime-setup(Node22/pnpm9 + clone + install, tsx hoist ✅) → ③ collector.env(0640 ✅) → ④ systemd unit 등록 → ⑤ enable --now(active) → ⑥ journalctl 부팅 로그(`markets=[futures_usdm] tasks=3` USDM-only 확정) → ⑦ G1 검증.

### G1 라이브 실증 (Supabase MCP, 2026-06-04)
| interval | 신규 row(>05-31 12:05) | 가장 최신 |
|---|---|---|
| 5m | 381,545 | **2026-06-04 11:25** |
| 1h | 56,616 | 2026-06-04 11:00 |
| 12h | 259 | 2026-06-04 00:00 |

→ **forward-fill 코드가 라이브에서 실제 INSERT 함을 실증.** 멈춰 있던 05-31 → 거의 현재(06-04 11:25)까지 채워짐. **G1 부분 충족** (전체 9 interval + same-IP ban 0 + 24~48h 지속은 후속).

---

## 라이브 실측 이슈 3건 (가동 중 적발)

### 이슈 1 — `getMaxRecordedAt` freshness 조회 25초 statement timeout → ✅ 해소
- **증상**: 첫 cycle에서 `[forwardFill:usdm-long] 12h freshness 조회 실패(skip): canceling statement due to statement timeout`.
- **원인** (EXPLAIN ANALYZE 실측): `(exchange, market_type, interval)` 필터 + `recorded_at DESC LIMIT 1` 쿼리가 기존 `natural_pk(exchange, market_type, **symbol**, interval, recorded_at)`로는 symbol 조건 부재 → 608 심볼 전수 스캔 + top-N sort = **24,996ms**. (코드 주석의 "저빈도라 부담 낮음" 가정이 빗나감 — 빈도 무관 인덱스 부적합.)
- **fix**: `(exchange, market_type, interval, recorded_at DESC)` 전용 인덱스 신설 (`20260604000001_m1_9_step3_freshness_index.sql`). **25초 → 5.861ms** (Index Only Scan, Heap Fetches 1). 기존 인덱스 3개 전부 보존(ADD not DROP). `docs/DB_SCHEMA.md` 갱신.
- ⚠️ Supabase MCP가 read-only mode라 인덱스 생성은 **사용자가 Dashboard SQL Editor에서 `SET statement_timeout='600s'` + CREATE INDEX** 직접 실행. 검증(EXPLAIN)은 Claude가 read로 수행.

### 이슈 2 — `/futures/data/basis` -1003 ban (별도 IP인데도) → 즉효 fix(피크 완화)
- **증상**: catch-up 중 basis에 집중된 `-1003`(418 IP ban / 429 "2400 req/min"). 다른 5 metric은 통과.
- **원인** (crypto-domain-expert 확정, 공식 change-log 2023-10-19): `basis`만 "1000 req/5min" 별도 카운터 조정 목록에서 **빠져** 일반 fapi weight 풀(2400/min)에 걸림. + (backend-infra) throttle이 task별 독립 클로저 → 3 task 0ms 동시 발화 합산 + 첫 catch-up(4일) 페이지 폭발 + 재시도 증폭. 메시지의 `10.119.x.x`는 Binance 내부 LB 라벨(우리 공인 IP에 실제 ban). **production worker(178.x) 무관**.
- **즉효 fix**: basis 전용 2400ms floor(25/min) + task staggered start(0/30/60s) + (이슈3 공통) TimeoutStopSec.
- ⚠️ code-reviewer W1: basis floor는 단일 클로저 공유라 "prev→basis 간격"을 벌림(과하게 안전 — basis-to-basis는 더 넓어짐). 부작용 = **cycle이 심볼 수 × 2400ms 팽창**(USDM ~450심볼 → interval당 +~18분). catch-up 동안만, tier=low라 허용. 근본 fix(per-metric lastCallAt)는 `[8-31]`.

### 이슈 3 — graceful shutdown 90초 초과 → SIGKILL → `failed` → 즉효 fix
- **증상**: `systemctl stop` 시 `executeHistoryBackfill` 진행 중이라 SIGTERM 후 90초(systemd 기본) 초과 → SIGKILL → `Failed with result 'timeout'`.
- **원인**: unit `TimeoutStopSec` 미설정 + `executeHistoryBackfill`에 abort 신호 없어 cycle(수분)을 중간에 못 끊음. 데이터 손상은 없음(멱등).
- **즉효 fix**: `KillSignal=SIGTERM` + `TimeoutStopSec=180`(증상 완화). 근본 fix(AbortSignal 협조적 취소)는 `[8-31]`.

---

## 즉효 fix 3종 (이번 세션 코드 변경, `@backend-infra-specialist` 구현 / `@code-reviewer` 0 Critical)

| # | 파일 | 변경 |
|---|---|---|
| 1 | `apps/collector-history/deploy/travis-collector-history.service` | `KillSignal=SIGTERM` + `TimeoutStopSec=180` |
| 2 | `packages/shared/src/poller/IPoller.ts` + `TierPoller.ts` | `PollTask.initialDelayMs?` optional (start/register 첫 tick만 적용, 미지정 0=회귀 0) |
| 2 | `apps/collector-history/src/poller/forwardFillTask.ts` | `STAGGER_STEP_MS=30000`, taskIndex × 30s 분산 |
| 3 | `packages/exchange-collectors/src/core/historyBackfillCore.ts` | `MetricFetcher.minReqIntervalMs?` + basis(USDM/COINM) 2400ms floor + `throttle(metricFloorMs)`=`max(minIntervalMs, metricFloorMs)` |
| - | DB | `20260604000001_m1_9_step3_freshness_index.sql`(이슈1) + `docs/DB_SCHEMA.md` |

**검증**: type-check 6패키지 ✅ / lint ✅ / worker **105 test 회귀 0** ✅ / code-reviewer **0 Critical**(W1~W4 전부 `[8-31]` 설계 입력값).

---

## 자문 종합
- **crypto-domain-expert**: basis = fapi weight 풀(2400/min) 별도 카운터(공식 change-log). 권고 basis 20~30/min. `10.119.x.x` = Binance LB 라벨. 본인 memory(`project_m1_9_step2c_coinm_history.md` 등) 정정.
- **backend-infra-specialist**: 두 이슈 root cause = "task별 독립 단위 ↔ 프로세스 전역 제약(IP 카운터/종료시한) 비협조" 한 뿌리. 근본 = shared limiter + AbortSignal(`[8-31]`).
- **code-reviewer**: 0 Critical, 회귀 0. W1(basis floor cycle 팽창)/W4(STAGGER group-relative)/S1(per-metric lastCallAt)/S3(AbortSignal 후 TimeoutStopSec 축소) → `[8-31]` 입력.

---

## Step 1 후속 — ⓒ per-metric throttle + [8-33] 금속 basis 제외 ✅ (2026-06-05)

> **선행 라이브 재진단 (DB 실측, 2026-06-05 05:05 UTC)**: collector 생존 ✅ (`history_futures_indicator` 5.13M row, 06-05 새벽까지). **단기봉 lag 2~3h 실측** (5m=02:35 / 1h=02:00 / 30m=01:00 vs now 05:05). COINM 0 row(롤아웃 전). `history_futures_kline` 0 row = forward-fill scope 밖(MetricFetcher 6종 전부 indicator metric, kline 은 TradingView 위젯 담당 — 정상).

### lag 진짜 원인 재진단 (roadmap 분해 전제 일부 정정)
- `forwardFillTask.ts`: `DEFAULT_TOTAL_REQ_PER_MIN=150` ÷ USDM 3 task = task당 50 → `minIntervalMs=1200ms`.
- short task cycle = (450심볼 × 6metric × 1200ms = 54분 + basis floor 패널티 9분) × 3 interval = **~3.15h** = lag 2.5h 정합.
- **결론**: lag 주범은 basis floor(ⓒ)가 아니라 "심볼수×metric÷reqPerMin = cycle 하한" 폴링 구조. ⓒ 는 ~14%(27분)만 개선. ⓐ token-bucket 도 평균 한도 같아 정상 cycle 못 줄임(catch-up burst 만). **단기봉 실시간 추종은 폴링 구조론 불가 = WS 영역**. forward-fill 목적은 history 누적이지 실시간 아님.
- **★ 사용자 결정 (2026-06-05)**: lag 1~3h 를 history 누적 목적상 **허용**. 실시간 5m 은 now_* 카드 담당. ⓒ + [8-33] 은 "정확화 + 청결" 로 한정 진행.

### ⓒ per-metric lastCallAt (`[8-31]`ⓒ 회수)
- `PerMetricThrottle` 클래스 신설 (`packages/exchange-collectors/src/core/perMetricThrottle.ts`) — 공통 floor 전역 1개 + metric 자체 floor(basis 2400ms)는 `Map<metricName, lastCallAt>` 로 그 metric 자신에만 적용. 단일 클로저 공유 버그(W1) 해소.
- 효과: basis 의 부당한 cycle 팽창(interval당 9분) 제거 = lag ~14% 개선. 과장 금지 주석 명시.

### [8-33] 금속/주식 선물 basis -4104 제외 (회수)
- `unsupportedMetricCache.ts` 신설 — -4104 응답 학습 캐시(reactive, in-memory Set, 키=`marketType:symbol:metric` interval 무관).
- **★ 라이브 교차검증으로 자문 가정 정정** (`external_api_live_smoke`): crypto-domain "underlyingType=INDEX 제외" 권고 → fapi 실측 결과 (a) 진짜 -4104 기준 = `contractType=TRADIFI_PERPETUAL`(77종 중 75종: 금속=COMMODITY/주식=EQUITY·KR_EQUITY/프리마켓=PREMARKET), (b) INDEX 2종(BTCDOM/ALL)은 PERPETUAL → basis **정상**(underlyingType 제외 시 false positive), (c) symbols 테이블 underlyingType 컬럼 부재(DB 변경 scope 밖) → reactive 캐시가 정공.
- **안전 경계**: `-4104` 만 학습, `-1003` rate limit 은 절대 학습 금지(정상 심볼 영구 skip 치명 버그 방지). 두 에러 경로(400 + 2xx envelope) substring 매칭.
- 효과: 75심볼 × basis 호출이 매 cycle 영구 제거 → IP quota 여유 + 로그 청결.

### 검증
- type-check 6패키지 ✅ / worker **110 test(105+5 신규, 회귀 0)** ✅ / lint ✅ / mixed-batch 불변 유지 ✅ / 순환 의존 0.
- code-reviewer **0 Critical** → commit 가능. Warning 3건(W1 -4104 두 경로 주석 / W2 reserve drift 하한 표현 / W3 테스트 상수 임의값 명시) **전부 반영 완료**.
- 변경 파일: `perMetricThrottle.ts`(신규) / `unsupportedMetricCache.ts`(신규) / `historyBackfillCore.ts` / `index.ts` / `historyBackfillThrottleSkip.test.ts`(신규 5 test).
- 회수: `[8-31]`ⓒ(+W1/S1 부분) · `[8-33]`(묘비).

### crypto-domain COINM 합산 산수 (다음 ⓐ/롤아웃 입력값, 2026-06-05)
- COINM PERPETUAL = **17 심볼**(USDM 450 의 3.8%, 금속/지수 0). 권고 shared bucket = 통계 5종 합산 150/min + basis 합산 30/min. dapi·fapi 같은 IP 카운터 공유 가정. → COINM 켜도 ban 0(단 ⓐ 의 basis 클로저 USDM·COINM 공유 필요).

---

## Step 2 후속 — ⓐ 프로세스 전역(opt-in) /futures/data token-bucket ✅ (2026-06-05)

> **`[8-31]`ⓐ 회수.** catch-up burst + 미래 COINM 합산 시 `/futures/data` IP quota(통계 1000 req/5min, basis fapi weight 풀 2400/min) 초과를 전역 카운터로 hard cap.

### 구현
- `FuturesDataRateLimiter` 신설 (`packages/exchange-collectors/src/core/futuresDataRateLimiter.ts`) — 순수 TokenBucket 2개: 통계 5종 **150/min**(1000 req/5min 의 75% 마진) + basis **30/min**(fapi weight 풀 2400/min 보호). path(`/futures/data/basis`)로 버킷 구분. 프로세스 전역 싱글톤. `acquire`=토큰 부족 시 await(throw 금지, 리필 보장 종료).
- `binanceFetch` opts `rateLimiterGroup?` 추가 — 매 시도(재시도 포함=실제 IP 요청) 전 acquire.

### ★ opt-in 설계 결정 (a vs b → **b 채택**)
- client.ts 는 worker·collector 공유 코어. **worker now-poller(perSymbolTask, batchPerSymbol 50ms)가 `/futures/data` 를 순간 ~1200 req/min 사용** → 150/min 전역 적용 시 worker cycle 폭증 + now_* 신선도 파괴.
- → `rateLimiterGroup` 미지정 시 비활성. collector fetcher 12개(USDM 6 + COINM 6)만 `"collector"` opt-in. worker 전 경로 무영향.
- **W1 생명줄 테스트**(fetch-mock): group 미지정 + `/futures/data` → 토큰 **불변**(worker 무영향) / group 지정 → 감소 / group 지정 + 비-`/futures/data`(OI=`/fapi/v1/openInterest` 별개 풀) → 불변. 직접 단언.
- OI 구분: `/fapi|dapi/v1/openInterest` 는 별개 weight 풀 → `/futures/data` 카운터 미포함(정확).

### S1 자동 해소
basis 전역 단일 버킷이 path 로 USDM·COINM basis 합산 통제 → `[8-31]` S1(basis 클로저 USDM·COINM 공유) 구조 소멸.

### PerMetricThrottle(ⓒ)와 공존
PerMetricThrottle=task 내부 per-metric 간격(sleep), token-bucket=프로세스 전역 합산 hard cap. backfill loop 에서 직렬 합산 → 둘 중 **느린 쪽이 실질 한도**(reqPerMin vs 150 중 min). 의도된 안전망(W2 주석 명시).

### 검증
- type-check 6패키지 ✅ / worker **122 test(110→116 ⓐ기본+6→122 W1+S1 +6, 회귀 0)** ✅ / lint ✅.
- code-reviewer **0 Critical** → commit 가능. W1(분기 테스트)/W2(이중대기 주석)/W3(`_resetFuturesDataRateLimiterForTest`)/S1(capacity<=0 console.warn+클램프) **전부 반영**. S2(거래소 추상화)→`[8-27]`/`[8-31]` deferred 한 줄, S3(AbortSignal 시 `acquire(signal?)`)→ⓑ 회수 대상.
- 변경 파일: `futuresDataRateLimiter.ts`(신규) / `client.ts` / `historyFetchers.ts`(USDM 6) / `coinmHistoryFetchers.ts`(COINM 6) / `index.ts` / `futuresDataRateLimiter.test.ts`(신규 12 test) / `historyBackfillCore.ts`(W2 주석).
- 회수: `[8-31]`ⓐ(+S1). **ⓐⓒ 완료 = COINM 롤아웃 전제 충족**(라이브 재배포·24~48h 검증 선행).

---

## 라이브 재배포 #1 (2026-06-05 07:01, commit 36708b4 = ⓐⓒ/[8-33])
- 사용자 SSH `sudo -u travis git pull`(ab9e087→36708b4 fast-forward, ⓒ+[8-33]+ⓐ 일괄) + restart.
- **DB 실측 (07:03)**: 5m lag 2.5h→**3분** / 1h→**3분** (극적 개선). 15m=183분·30m=93분은 재시작 직후 과도기(첫 cycle 진행 중, interval freshness anchor 독립). **정상 상태 lag 은 24~48h 안정 후 측정** (단정 보류).
- 부팅 정상(`markets=[futures_usdm] tasks=3`, 608 심볼, COINM off).
- ⚠️ **SIGKILL 실측**: restart stop 단계 `stop-sigterm timed out → SIGKILL → Failed`. = ⓑ 미구현이라 cycle(수분) 미중단. 멱등 무해. → ⓑ 착수 트리거.

## Step 후속 — ⓑ AbortSignal 협조적 취소 ✅ (2026-06-05)
> **`[8-31]`ⓑ 회수.** shutdown 시 진행 중 cycle 즉시 graceful 중단 → SIGKILL/`Failed` 제거.

### 구현 (signal 배선 4곳 + 2 설정)
- `abortableSleep.ts` 신설 — signal 미지정=평범 setTimeout / 지정=타이머 vs abort 먼저 오는 쪽 **resolve(reject 아님, graceful)**, 양쪽 cleanup(clearTimeout+removeEventListener).
- `executeHistoryBackfill` — `ExecuteBackfillDeps.signal?` + interval/symbol/page 3경계 `aborted` 체크 → graceful return(부분 결과, throw 0). throttle sleep→abortableSleep. `buildResult()` 클로저(정상/부분 동일 형태).
- `TokenBucket.acquire(path, signal?)` + `RateLimiterClock.sleep(ms, signal?)` — 토큰 고갈 대기 중 abort 즉시 탈출.
- `client.ts` — `acquire(path, opts.signal)` + fetch 직전 aborted 체크 + catch 내 abort 는 재시도 backoff 우회(shutdown 지연 방지).
- **signal 을 `HistoryFetchWindow` 에 실음** → fetcher 12개 시그니처 불변(변경 최소화 trade-off, code-reviewer 수용).
- `collector-history/index.ts` — `AbortController` 생성, shutdown 핸들러에서 `poller.stop()` **전에** `abort()` 발사(순서 핵심: abort 먼저→inFlight 수초 내 풀림→stop 대기 짧음).
- `.service` — `TimeoutStopSec 180→30`(abort 즉시 종료라 안전망만). **재배포 시 cp + daemon-reload 필요**.

### 안전성
- **worker 무영향**: signal/group 전부 optional → worker `runHistoryBackfill`·now-poller 는 `opts.signal?.aborted` 항상 falsy → abort 분기 skip. 테스트 (c) signal 미지정 시 18 fetch 정상 완료 실증.
- **멱등 보존**: 중간 중단해도 다음 cycle 이 `getMaxRecordedAt` anchor 부터 재개. mixed-batch 불변(market별 별도 배치) 유지.
- **TOCTOU race**: aborted 체크와 fetch 사이 abort 끼면 fetch 1개 발사 가능하나 멱등이라 무해(테스트 `<=2` 관용 반영).

### 검증
- type-check 6패키지 ✅ / worker **130 test(122→130, +8: abortableSleep 5 + historyBackfillAbort 3, 회귀 0)** ✅ / lint ✅.
- code-reviewer **0 Critical** → commit 가능. Warning 3(W1 sleep 가드 순서/W2 테스트 위치=worker runner 재사용 일관/W3 fetchCalls<=2 race 관용) 전부 위생·수용. **★ S1 관찰 포인트**: `retryOnTransient`(upsert 재시도)는 abort 미인지 → 라이브 30초 SIGKILL 재발 시 upsert retry 1순위 의심.
- 변경 파일: `abortableSleep.ts`(신규) / `historyBackfillCore.ts` / `futuresDataRateLimiter.ts` / `client.ts` / `historyFetchers.ts` / `coinmHistoryFetchers.ts` / `index.ts` / `collector-history/index.ts` / `forwardFillTask.ts` / `.service` / 테스트 2(신규).
- **ⓐⓑⓒ 완료 = COINM 롤아웃 전제 충족. 잔여 ⓓ circuit breaker(📋, 차단 아님).**

---

## 후속 세션 인계 (남은 작업)

0. **(다음 라이브 재배포 #2) ⓑ 반영** — `.service` 변경 포함이라 절차에 cp 추가: `sudo -u travis git -C /opt/travis pull` → `cp /opt/travis/apps/collector-history/deploy/travis-collector-history.service /etc/systemd/system/` → `systemctl daemon-reload` → `systemctl restart travis-collector-history`. **검증 = restart 시 SIGKILL 없이 `inactive`(not `Failed`)**.
1. **즉효 fix 배포 + 재가동 검증**: 서버 `git pull`(`sudo -u travis`) + `travis-collector-history.service` 재업로드 + `systemctl restart` → staggered start 로그 + basis `-1003` 감소 + shutdown 깨끗(`inactive`, not failed) 확인. **(ⓒ/[8-33] 도 이 재배포에 포함됨 — basis 호출 감소 + cycle 단축 로그 확인.)**
2. **근본 fix `[8-31]`** (다음 세션 별도 작업): 프로세스 전역 shared `/futures/data` rate limiter + `executeHistoryBackfill` AbortSignal 협조적 취소 + per-metric lastCallAt(basis-to-basis 정확화) + AbortSignal 도입 후 `TimeoutStopSec` 180→축소. `@code-reviewer` 패스 권장. **worker 105 test 회귀 0 필수**(코어 공유).
3. **USDM 24~48h 안정성 + site=DB 정밀 대조(G2)**: 9 interval 전부 + same-IP ban 0 + Binance USDM 사이트 BTC/ETH 5m~1d 값 일치.
4. **COINM 롤아웃**: `[8-31]` 합산 req/min 재확인(crypto-domain-expert — USDM만으로도 ban 났으므로 6 task 산수 필수) → `FORWARD_FILL_COINM=1` → COINM `_PERP` INSERT + 사이트(BTCUSD_PERP OI=contract 단위) 대조.
5. **G1~G5 종단 게이트** (ROADMAP §M1.9) + `M1.9-complete.md` 신설(G5).
6. **추가 적발(2026-06-04 배포 중)**:
   - **금속 선물 basis `-4104`** (`[8-33]`): XAU/XAG/XPT/XPD basis 미지원 → graceful skip 중이나 fetch 대상 사전 제외 권장.
   - **재배포 git pull 함정**: `/opt/travis`는 travis 소유 → `git pull`은 **`sudo -u travis`로** (root 직접 시 `dubious ownership` 거부). DEPLOY-RUNBOOK §재배포에 반영 완료.
   - **즉효 fix 배포 검증(2회차, ab9e087 PID 8064, 12:52~54 실관찰)**:
     - ✅ **staggered start 작동**: progress 로그 `5m(12:53:25)→1h(12:53:55)→12h(12:54:22)` ~30s 간격(이전 0s 동시 발화 대비).
     - 🟡 **basis `-1003` 빈도 감소하나 잔존**: 이전 매 줄 다발 → progress 사이 1~2개. catch-up(8/608 재시작) 중 압박 최대 구간 + basis floor가 단일 클로저(W1)라 완전 차단 아님. catch-up 1바퀴 완료 후 증분 전환 시 급감 예상. **완전 해소는 `[8-31]` shared limiter**.
     - 🟡 **TimeoutStopSec=180 미검증**: 12:52:16 SIGKILL은 직전 7941(옛 90s service) 건. 최신 8064 stop 시 검증 예정.
     - AIUSDT basisRate -0.26 의심 로그 = 저유동 신규 코인 정상, sanity guard warn만 graceful 통과(정합 영향 0).
