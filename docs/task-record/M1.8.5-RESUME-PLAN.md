# M1.8.5 Resume Plan — /clear 후 새 세션 재개용 완전 가이드

> **상태**: 🟡 진행 중 (2026-05-31 진입, Step 1 docs 신설 단계)
> **선행**: M1.8 ✅ 완료 (2026-05-28, 28 commit, 종단 게이트 G1~G5 전부 통과) — 단일 진실 원천 `docs/task-record/M1.8-complete.md`
> **목적**: 사용자가 `/clear` 후 새 세션에서도 M1.8.5 의 진행 상태 / 잔여 작업 / 사용자 결정 영역 / 다음 명령을 **30초 안에** 회복 가능하도록 단일 진실 원천 제공.
> **사용 방법**: 새 세션 진입 시 본 파일을 가장 먼저 Read → §0 한 줄 요약 + §1 현재 시점 + §3 다음 명령 순서.

---

## 0. 한 줄 요약

> **"M1.8 ✅ 종결 (2026-05-28) 후 8.3c (β) 이월 작업 = M1.8.5 history backfill 진입. `@roadmap-milestone-manager` 분해 6-step (Step 1 docs ✅ / Step 2 schema ✅ / Step 3 fetcher+normalize+upsert+test ✅ / Step 4 loop 코드 ✅ **+ 배포 보류** 2026-05-31 / Step 5 실 backfill + sliding window 결정 / Step 6 종단 게이트 G1~G5 + complete.md). 누적 예상 9~12h. 1차 backfill = 6 metric × 9 interval × 608 symbols × 14일 + 5m 9페이지 윈도잉 ≈ **57~72K REST** / 25M row / 2.5GB Supabase / **~9.5~12h @ 100 req/min** (D-Q1 150 → crypto-domain margin 권고로 보수적 100, quota 충돌 해소 = 조건부 GO). 다음 첫 명령 = **사용자 활성화 go → `index.ts` dryRun:false 1줄 전환 + Hetzner SSH 배포** (Step 4 코드/3 자문 완료, hard-to-reverse 라 명시 go 대기). 단일 진실 `M1.8.5-step4-deploy.md`."**

---

## 1. 현재 시점 (2026-05-31)

### 1.1 마지막 commit (M1.8 종결 시점)

```
89e2d15 docs(m1.8-complete): 🎉 M1.8 완료 선언 — 종단 게이트 G1~G5 전부 통과 (FG-7/FG-8)
7d76016 docs(m1.8-final-gate): FG-5 0 Critical 자문 ✅ (게이트 G3 PASS)
44bfa3e docs(m1.8-final-gate): FG-2~FG-6 검증 ✅ (크래시 복구 세션 재개)
f81c434 docs(m1.8-step4-e): spot full ticker fix 배포+재검증 ✅ (spot null 0%)
c919190 fix(m1.8-step4-e): spot price_change_pct full ticker 복귀 (종단 게이트 G1 hotfix)
```

본 M1.8.5 진입 commit (예정) = Step 1 의 4 docs 신설/갱신 일괄 commit (`docs(m1.8.5-step1)`).

### 1.2 6-step 매핑 (`@roadmap-milestone-manager` 분해 채택 2026-05-31)

| Step | 작업 | 상태 | 예상 |
|---|---|---|---|
| **1** | 세션 진입 + RESUME-PLAN + ROADMAP §M1.8.5 + [8-15] 마커 + memory | ✅ **완료** (2026-05-31, commit `134d4d7`) | ~30분 |
| **2** | Schema migration (D22=A SQL 3단, **자연 키 UNIQUE INDEX 신설** — PK 재구성 X) + DB_SCHEMA.md 갱신 + M1.8.5-step2 task-record | ✅ **완료** (2026-05-31, Dashboard 수동 실행) | ~45분 |
| **3** | fetcher 6종 + normalize 6종 + dataService upsert + vitest 14 + live smoke | ✅ **완료** (2026-05-31) — 77 test PASS / live smoke 6/6 200 OK / D-Q2=9페이지 / code-reviewer C1+W1+W4 반영. task-record `M1.8.5-step3-fetchers.md` | 3~4h |
| **4** | runHistoryBackfillTask 실 호출 path + dryRun:false 전환 + Hetzner deploy | 🟡 **코드 ✅ / 배포 보류** (2026-05-31, 3 자문 통과, quota 조건부 GO, D-Q5 + 활성화 go 대기). task-record `M1.8.5-step4-deploy.md` | 1~1.5h |
| **5** | 1차 실 backfill 2.97h + 5 검증 쿼리 + sliding window 결정 | ⏳ 대기 | ~3h |
| **6** | 종단 게이트 G1~G5 + M1.8.5-complete.md + 7 docs sync | ⏳ 대기 | 1.5~2h |

**총 예상**: 9~12h (CTO 7-step 안 대비 -1h, Step 1·2·3 묶음 분리로 commit boundary 명확화)

### 1.3 산출물 위치 (M1.8 자산 — M1.8.5 진입 시 재활용)

- **historyBackfillTask** (dryRun 영구 가동 중): `apps/worker/src/poller/tasks/historyBackfillTask.ts` — Hetzner since 2026-05-27 06:47:47 UTC. 5분당 시뮬레이션 출력 정확 (608 symbols / 27,360 REST / 20.5M row / 2.28h / 1.91GB).
- **worker bootstrap**: `apps/worker/src/index.ts` — 5 task 중 `historyBackfillTask` 가 `dryRun: true` 안전 우회. Step 4 에서 1줄 `false` 전환.
- **canonical-metrics.md §5**: 13셀 매트릭스 (M1.8 G1) → M1.8.5 G1 의 63셀 매트릭스 base.
- **D20=C / D21=B / D22=A**: 사용자 결정 묘비 (`[8-12]` / `[8-13]` / `[8-14]` deferred 묘비 완료).
- **[8-15]**: 본 마일스톤 entry, Step 1 진입 시 카테고리 마커 갱신.

---

## 2. 6 step 분해 상세 (단일 진실 원천)

### Step 1 — 세션 진입 + 단일 진실 원천 신설 (🟡 진행 중, ~30분)

**산출물**:
- ➕ `docs/task-record/M1.8.5-RESUME-PLAN.md` (본 파일)
- ➕ `docs/ROADMAP.md §M1.8.5` 신규 섹션
- ✏️ `docs/deferred-task.md` `[8-15]` 카테고리 마커 갱신 ("다음 마일스톤" → "진행 중")
- ➕ `MEMORY.md` 인덱스 + `project_m1_8_5_in_progress.md` 신설

**검증 기준**:
- (a) RESUME-PLAN 8 섹션 (한 줄 요약 / 현재 시점 / step 분해 / 새 세션 진입 가이드 / 영구 규율 / D 시리즈 / 일관성 매트릭스 / 종료 조건) 존재
- (b) ROADMAP §M1.8.5 헤더 + 완료 기준 + 종단 게이트 G1~G5 정의 존재
- (c) `[8-15]` 카테고리 마커 갱신 확인
- (d) `MEMORY.md` 에 M1.8.5 진행 중 인덱스 1줄 추가

**의존성**: 없음 (M1.8 ✅ 종결 후 자연 진입)

**사용자 결정 영역**: **D23** = 본 6-step 분해 채택 여부 (✅ **2026-05-31 채택 완료**)

### Step 2 — Schema migration (D22=A SQL 3단, ~45분) ✅ **완료 (2026-05-31)**

> **표현 정정 (2026-05-31)**: "UNIQUE INDEX 재구성" → "**자연 키 5축 UNIQUE INDEX 신설**". 사전 확인 결과 `history_futures_indicator` 는 이미 `id BIGINT IDENTITY` surrogate PK 를 가진 fact-table — D22=A 는 PK 변경이 아니라 자연 키 UNIQUE INDEX 추가 (기존 id PK + 4축 lookup INDEX 와 공존, 3축 fact-table 패턴 완성).
>
> **적용 경로 (사후)**: Supabase MCP `apply_migration` 이 read-only mode 차단 → (A) 마이그레이션 파일 `supabase/migrations/20260531000001_m1_8_5_step2_history_interval.sql` 신설 + Supabase Dashboard SQL Editor 수동 실행 패턴 채택. 단일 진실 원천: `docs/task-record/M1.8.5-step2-schema-migration.md`.

**산출물**:
- Supabase MCP `mcp__supabase__apply_migration` 3단 SQL:
  ```sql
  ALTER TABLE history_futures_indicator
    ADD COLUMN interval VARCHAR(5) NOT NULL DEFAULT '1d';
  CREATE UNIQUE INDEX history_futures_indicator_natural_pk
    ON history_futures_indicator (exchange, market_type, symbol, interval, recorded_at);
  ALTER TABLE history_futures_indicator
    ALTER COLUMN interval DROP DEFAULT;
  ```
- ✏️ `docs/DB_SCHEMA.md §history_futures_indicator` 갱신 (interval 컬럼 + UNIQUE INDEX 명시)
- ➕ `docs/task-record/M1.8.5-step2-schema-migration.md` 신설

**검증 기준**:
- (a) `\d history_futures_indicator` 에 `interval VARCHAR(5) NOT NULL` 존재 + DEFAULT 없음
- (b) UNIQUE INDEX `(exchange, market_type, symbol, interval, recorded_at)` 존재
- (c) `pnpm rls-check` 13/13 PASS (Supabase MCP execute_sql 동등 쿼리)
- (d) 기존 row 0 건 (테이블 비어있음 사전 확인) — `SELECT COUNT(*)` = 0

**의존성**: Step 1 완료 (RESUME-PLAN 단일 진실 원천 확보)

**사용자 결정 영역**: 없음 (D22=A 확정)

### Step 3 — fetcher 6종 + normalize 6종 + unit test (2.5~3.5h)

**산출물**:
- ✏️ `apps/worker/src/adapters/binance/BinanceUsdmAdapter.ts` 또는 별도 history fetcher 파일에 6종 신설:
  - `fetchOpenInterestHistory(symbol, period, limit)`
  - `fetchTopLongShortAccountHistory(symbol, period, limit)`
  - `fetchTopLongShortPositionHistory(symbol, period, limit)`
  - `fetchGlobalLongShortHistory(symbol, period, limit)`
  - `fetchTakerLongShortHistory(symbol, period, limit)`
  - `fetchBasisHistory(pair, contractType, period, limit)`
- ➕ `apps/worker/src/adapters/binance/normalize/historyFutures.ts` (recorded_at + interval + 6 metric 컬럼 매핑 + 위생 #5 sanity guard)
- ➕ vitest 12 신규 (fetcher 6 × happy/edge 2)
- ➕ `docs/task-record/M1.8.5-step3-fetchers.md` 신설

**검증 기준**:
- (a) `pnpm -F @travis/worker type-check` + `lint` PASS
- (b) `pnpm -F @travis/worker test` 75 PASS (기존 63 + 신규 12)
- (c) **각 fetcher 가 Binance 공식 endpoint 1회 live smoke** (`5m` × `limit=10` 작은 호출, IP quota 영향 무시) — 6 endpoint 모두 200 OK + payload shape 검증
- (d) sanity guard (위생 #5 극단값 null 처리) 단위 테스트 ≥ 1건

**의존성**: Step 2 (interval 컬럼 존재 필수 — normalize 가 interval 매핑)

**사용자 결정 영역**: **D24** = backfill 중 perSymbol+ticker24hr 동시 가동 — (A) 동시 (권장, quota 마진 ≥30% 검증됨) / (B) 일시 중단

### Step 4 — runHistoryBackfillTask 실 호출 path + dryRun:false 전환 + Hetzner deploy (1~1.5h)

**산출물**:
- ✏️ `apps/worker/src/poller/tasks/historyBackfillTask.ts` 의 `runHistoryBackfillTask` 실 호출 path:
  - (a) for-loop over 9 interval × 608 symbol
  - (b) Step 3 fetcher 호출
  - (c) Step 3 normalize
  - (d) `dataService.upsertHistoryFuturesIndicator(batch)` (또는 동등 메서드)
  - (e) IP quota 모니터링 로그 (X-MBX-USED-WEIGHT-1M)
  - (f) progress journal 5분당 1회
- ✏️ `apps/worker/src/index.ts` bootstrap 1줄 `dryRun: false` + 가동 시간대 가드 (D25 결과 반영)
- ➕ `docs/task-record/M1.8.5-step4-deploy.md` 신설

**검증 기준**:
- (a) `pnpm -F @travis/worker type-check` + `lint` PASS
- (b) worker test 75 PASS
- (c) Hetzner `systemctl restart travis-worker` 후 5분 dry-run 시뮬레이션 출력 종료 + 실 호출 진입 로그 확인
- (d) NRestarts 0회 (10분 관찰)

**의존성**: Step 3 (fetcher + normalize 완성)

**사용자 결정 영역**: **D25** = 가동 시간대 — (A) 24/7 즉시 / (B) 야간 cron 3 야간 / (C) fire-and-forget 2.97h 1회 (권장, 5min quota 의 ~4% 소비)

### Step 5 — 1차 실 backfill (~2.97h) + 5 검증 쿼리 + sliding window 결정 (~3h, 대부분 대기)

**산출물**:
- (실행 자체는 코드 0줄) Hetzner journal log 캡처 + Supabase MCP `execute_sql` 5종 검증 쿼리 결과
- **sliding window 정책 결정 docs** (`docs/canonical-metrics.md §6` 신설 또는 `[8-18]` deferred 등재 — 구현 자체는 본 마일스톤 밖)
- ➕ `docs/task-record/M1.8.5-step5-backfill-run.md` 신설

**검증 기준 (5 검증 쿼리)**:
- (a) `SELECT COUNT(*) FROM history_futures_indicator WHERE recorded_at > now() - interval '14 days'` ∈ [20M, 28M] (canonical ~25M 의 ±15%)
- (b) `SELECT interval, COUNT(*) FROM history_futures_indicator GROUP BY interval` 9 행 모두 존재
- (c) `SELECT COUNT(DISTINCT symbol)` = 608±5
- (d) IP quota 위반 0건 (worker log `grep -c "429\|418"` = 0)
- (e) NRestarts 0회 + perSymbolTask cycle 충돌 0건 (M1.8 의 ~453 회/6h baseline 유지) + Supabase DB 용량 증가 ≤ 3GB

**의존성**: Step 4 (dryRun:false 가 prod 진입)

**사용자 결정 영역**: **D26** = 14일 sliding window archive 정책 — (A) Supabase cron + DELETE 일배치 / (B) PostgreSQL native PARTITION BY RANGE / (C) 보류 → `[8-18]` 등재 (권장, deferred-decision 원칙 정합 — 운영 1주 데이터 없이 결정 금지)

### Step 6 — 종단 게이트 G1~G5 + 마일스톤 종료 (1.5~2h)

**산출물**:
- ➕ `docs/task-record/M1.8.5-complete.md` 신설 (M1.8-complete.md 패턴 미러링)
- ✏️ `docs/ROADMAP.md §M1.8.5` ✅ 마커
- ✏️ `docs/PRD.md §6` 갱신 (M1.8.5 ✅)
- ✏️ `docs/deferred-task.md` `[8-15]` 묘비 처리
- ➕ `MEMORY.md` `project_m1_8_5_complete.md` 신설 + 인덱스 갱신
- ✏️ `docs/M2-plan.md` "현재 위치" 갱신
- ✏️ `docs/canonical-metrics.md §5` 63셀 매트릭스 ✅ 마커

**종단 게이트 G1~G5**:
- **G1 — 63셀 사이트=DB 검증**: 7 metric × 9 interval 매트릭스에서 **사용자 표본 5셀** (BTC/ETH 2 symbol × OI/funding/lsr 3 metric × 1h interval, 사용자 자율 선택) Binance 공식 사이트 차트와 DB row 의 값 일치 (오차 < 1%) — M1.8 의 13셀 패턴 미러링
- **G2 — 자동 게이트**: rls-check 13/13 + type-check 4패키지 + lint + test 75 PASS + 1차 backfill 5 검증 쿼리 PASS
- **G3 — 0 Critical 자문**: `@code-reviewer` + `@backend-infra-specialist` (대량 적재 quota/메모리) + `@crypto-domain-expert` (canonical 정합) 3자문 0 Critical
- **G4 — deferred 일관성**: `[8-15]` 묘비 + `[8-18]` sliding window 신규 entry (D26=C 채택 시) + 신규 회수 0건
- **G5 — 마일스톤 종료**: 7 docs ✅ 마커 + memory 전환

**의존성**: Step 5 (실 backfill 완료 + 검증 통과)

**사용자 결정 영역**: 없음 (검증 통과 시 자동 종료)

---

## 3. 새 세션 진입 시 첫 5분 가이드

### 3.1 첫 명령

```powershell
# 1) 현재 상태 확인 — 마지막 commit 가 M1.8.5 step N 진행 commit 이어야 함
git log --oneline -5

# 2) 본 plan 파일 위치 확인
Get-ChildItem docs/task-record/M1.8.5-RESUME-PLAN.md

# 3) 다음 Step 진입 — 본 파일 §2 의 해당 Step 행 + 산출물 + 검증 기준 확인
#    Step 1 ✅ 완료 후 Step 2 (schema migration) 진입:
#      → Supabase MCP `mcp__supabase__apply_migration` SQL 3단 적용
#      → docs/DB_SCHEMA.md 갱신
#      → docs/task-record/M1.8.5-step2-schema-migration.md 신설
```

### 3.2 D 결정 영역 활성화 시점

- Step 3 진입 시 사용자에게 D24 확인 (권장: A 동시 가동)
- Step 4 진입 시 사용자에게 D25 확인 (권장: C fire-and-forget 2.97h)
- Step 5 진입 시 사용자에게 D26 확인 (권장: C 보류 → `[8-18]` 등재)

---

## 4. 영구 규율 재확인 (M1.8 에서 검증된 규율 — M1.8.5 도 동일)

> **CLAUDE.md "Docs 기록 의무 (substep 단위)"** (`feedback_docs_record_per_substep`): 각 step 진행 시 task-record + deferred-task + ROADMAP 즉시 기록. 한 commit 끝에 몰아 기록 금지. M1.8 §8.1 task-record 누락 사례 후 사용자 영구 규율 강조.

> **CLAUDE.md "애매하면 가정 전에 보고"** (`feedback_ask_before_assume`): 모르는/모호한 사안은 (a) 직접 검색 (b) 사용자 보고 (c) 승인 3단계 강제. M1.8.5 진입 시점에도 동일 적용.

> **CLAUDE.md "한 번에 하나의 작업"**: scope 넓히지 마. Step 진입 중 발견하는 부수 개선은 deferred-task 등재 후 다음 step 또는 M2 영역.

> **CLAUDE.md §데이터 위생 #9 "사이트 = DB 진실 일치 원칙"**: M1.8 의 13셀 → M1.8.5 의 63셀 시계열까지 확장. Binance 공식 사이트 차트와 DB row 값 일치가 G1 검증 본질.

**M1.8.5 Step 단위 의무**:
1. `docs/task-record/M1.8.5-stepN-*.md` 즉시 보강 (해당 step 의 산출물 + 검증 결과)
2. `docs/deferred-task.md` 회수/신설 즉시 등재
3. `docs/ROADMAP.md §M1.8.5` 마커 즉시 갱신
4. commit message 에 step 명시 (예: `feat(m1.8.5-step3): fetcher 6종 + normalize + 12 vitest`)

---

## 5. 사용자 결정 영역 D23~D26

| D# | Step | 결정 내용 | 권장안 | 상태 |
|---|---|---|---|---|
| **D23** | Step 1 | 본 6-step 분해 채택 여부 | 채택 | ✅ **2026-05-31 채택 완료** |
| **D-Q1** | Step 3 | backfill rate (req/min) | **150 req/min** (/futures/data/* quota 마진 30%) | ✅ **2026-05-31 채택** (자문 결과 반영) |
| **D-Q3** | Step 3 | 동일 키 처리 — 자연 머지 vs 별도 row | **별도 row + ON CONFLICT** (Binance raw timestamp 보존) | ✅ **2026-05-31 채택** |
| **D-Q4** | Step 3 | normalize.ts 분리 여부 | **분리 X** — historyFutures.ts 만 신설 (scope 좁히기, 기술부채 `[8-19]` 이월) | ✅ **2026-05-31 채택** |
| **D-Q2** | Step 3 내 | 5m interval page 분할 | **limit=500 최대 → 5m 14일=4032행 → 9 페이지** (startTime/endTime 윈도잉) | ✅ **2026-05-31 실측 확정** (live smoke: 500 요청 → 정확히 500행 반환) |
| **D24** | Step 4 | backfill 중 perSymbol+ticker24hr 동시 가동 | (A) 동시 — D-Q1 의 150 req/min 채택으로 자연 안전 | ⏳ Step 4 진입 시 (D-Q1 정합으로 (A) 권장 강화) |
| **D25** | Step 4 | 1차 backfill 가동 시간대 | (C) fire-and-forget 1회 | ✅ **2026-05-31 채택** |
| **D-Q5** | Step 4 | 배포 전략 (IP 격리) | (B) 같은 IP + 100 req/min + 429 graceful — quota 안전 확인(725<1000). (A) [8-20] 별도 IP 는 M2+ | ⏳ **활성화 go 시 사용자 확정** (권장 B) |
| **활성화** | Step 4→5 | dryRun:false 전환 + SSH 배포 | 6h/25M row/production IP 공유 = hard-to-reverse → 사용자 명시 go 필수 | ⏳ **사용자 go 대기** |
| **D26** | Step 5 | 14일 sliding window archive 정책 | (C) 보류 → `[8-18]` 등재 — 운영 1주 후 재결정 (deferred-decision 원칙) | ⏳ Step 5 진입 시 |

---

## 6. 본 plan 파일의 영구 가치

1. **세션 재개 비용 최소화**: `/clear` 후 30초 이내 맥락 회복.
2. **사용자 결정 영역 영구화**: D23~D26 가 deferred 와 본 plan 양쪽에 기록 → 어느 세션이든 동일 컨텍스트.
3. **명령 예시 직접 제공**: PowerShell + Supabase MCP + Hetzner deploy 절차 모두 명시.
4. **마일스톤 종료 후에도 가치**: 본 plan 의 §2 (6-step 분해) + §3 (새 세션 진입 가이드) 가 M1.9 또는 M2 Step 0 의 유사 패턴 진입 시점에 reference.

---

## 7. 관련 docs 일관성 보장 매트릭스

본 plan 파일과 동일 정보가 다른 docs 에 반영되었는지 추적:

| 정보 | M1.8.5-RESUME-PLAN | M1.8.5-stepN-*.md | deferred-task | ROADMAP §M1.8.5 | PRD §6 | M2-plan | canonical-metrics §5/§7.2 | memory |
|---|---|---|---|---|---|---|---|---|
| Step 1 진행 중 | §1.2 | step1 파일 없음 (본 RESUME-PLAN 이 step1 산출물) | `[8-15]` 마커 갱신 | §M1.8.5 헤더 + 6-step 표 | "M1.8.5 진행 중" (Step 6 에서 갱신) | "현재 위치" Step 6 에서 갱신 | (Step 5 G1 base) | `project_m1_8_5_in_progress.md` |
| D23 채택 (2026-05-31) | §5 | step1 (본 RESUME-PLAN) | — | (cross-link in 본 §M1.8.5) | — | — | — | `project_m1_8_5_in_progress.md` |
| D24/D25/D26 결정 시점 | §5 | 각 step 파일 | (D26 채택 시 `[8-18]` 신설) | (cross-link) | — | — | (D26 cross-link) | — |
| 6-step 분해 단일 진실 | §2 | (각 step 진입 시 본 §2 참조) | — | §M1.8.5 표 | (요약만) | (cross-link) | — | — |
| 종단 게이트 G1~G5 | §2 Step 6 | step6 파일 | — | §M1.8.5 완료 기준 | — | — | §5 63셀 매트릭스 | — |
| 1차 backfill 5 검증 쿼리 | §2 Step 5 + §3 | step5 파일 | — | (cross-link) | — | — | — | — |
| 영구 규율 재확인 | §4 | — | — | — | — | — | — | — |

**원칙**: 본 plan 파일이 단일 진실 원천. 다른 docs 는 cross-link 만 유지 (정보 중복 시 본 파일이 우선).

---

## 8. 본 plan 의 종료 조건

- M1.8.5 종단 게이트 G5 통과 시 (Step 6 종료)
- 본 plan 파일은 historical reference 로 보존 (삭제 X, M1.8-RESUME-PLAN 패턴 미러링)
- 다음 마일스톤 (M1.9 또는 M2 Step 0) 의 RESUME-PLAN 신설 시 본 파일 패턴 미러링

---

## 9. 비전공자 한 줄 요약

> **"M1.8 마일스톤 (선물 현재 시점 카탈로그 + 사이트=DB 일치) 가 완료된 후, 사장님이 자주 보실 '지난 14일 funding 추세' 같은 시계열 질문에 답할 수 있게 식자재 창고에 과거 데이터를 채우는 작업. 6 단계로 쪼개졌고 9~12시간 예상. 결정 4건 중 1건 (D23) 이미 채택됐고, 나머지 3건 (D24/D25/D26) 은 각 step 진입 시 권장안 함께 보고드림. 본 RESUME-PLAN 은 작업 중 `/clear` 가 나도 30초 안에 컨텍스트 회복 가능하게 만드는 단일 진실 원천."**
