// TRAVIS 프론트엔드 Next.js 설정.
// Next.js 16은 Turbopack이 기본 활성 — 현재는 빈 config로 충분.
// 추후 turbopack alias, transpilePackages 등이 필요해지면 여기에 추가.
import type { NextConfig } from "next";

const config: NextConfig = {};

export default config;
