# M1.9 — history forward-fill 지속성 + COINM 확장 ✅ COMPLETE

> **상태**: ✅ **완료 (2026-06-06)**. 종단 게이트 G1~G5 전부 통과.
> **단일 진실 원천**: 본 파일 (Step별 상세는 `M1.9-step0/step1/step2-forward-fill/step3-rollout.md`).
> **선행**: M1.8.5 ✅ (2026-06-01, USDM 과거 14일 1회 backfill) → M1.9 = 그 history 를 **미래로 계속 자라게** + COINM(dapi) 확장.
> **다음**: M2-plan §Step 2 (본인 실사용 + 베타테스터 피드백 수집).

---

## 0. 한 줄 요약

> **"별도 Hetzner 서버(49.13.138.121, 별도 IP)로 USDM+COINM forward-fill 을 24/7 가동 — M1.8.5 가 05-31 에 정지시킨 history 를 다시 자라게 했다. USDM 23h+ 무중단(NRestarts=0) + USDM ~50셀·COINM 24셀 site=DB 소수점 완전 일치 + ⓑ AbortSignal graceful 종료 2회 검증. 가동 중 basis `-1003` 1171회/24h 의 정체를 'Binance LB 노드 weight 풀 혼잡(우리 위반 아님, basis weight 0)'으로 규명해 task-record 의 과장된 'ⓐ로 구조 해소' 기록을 정정했다."**

---

## 1. Step 요약 (0~3)

| Step | 내용 | 완료 |
|---|---|---|
| **Step 0** | `[3-68]` transient_error → auth/quota/transient 3분류 | 2026-06-02 |
| **Step 1** | 별도 collector worker 인프라 추출 (`packages/exchange-collectors` + `apps/collector-history`) | 2026-06-02 |
| **Step 2** | forward-fill 구현 (USDM+COINM 일반화, `getMaxRecordedAt` 증분 윈도잉) | 2026-06-04 |
| **Step 3** | 2번째 Hetzner 서버 라이브 롤아웃 + 종단 검증 | 2026-06-04~06 |

핵심 결정 (2026-06-01): 방식 A(주기적 증분 backfill) + 별도 IP 서버(same-IP `-1003` ban 회피) + COINM 과거 backfill 생략(forward-fill 1달 누적이 대체) + USDM 먼저 검증 후 COINM 순차.

---

## 2. 종단 게이트 G1~G5 결과

- **G1 — 지속 누적 + 멱등 + ban 0** ✅
  - USDM: 9 interval 전부 05-31 정지점 → 06-06 까지 누적 지속 (5.42M row), `NRestarts=0` 23h+ 무중단.
  - COINM: 롤아웃(06-06 07:32) 후 INSERT 시작 (1h 18심볼 / 5m / 12h).
  - 멱등: BTCUSDT 5m natural key 중복 0.
  - same-IP ban: **우리 공인 IP(49.13.138.121) ban 0회** — basis `-1003` 의 `10.119.x.x` 는 Binance 내부 LB 사설 IP(우리 IP 아님, §4 참조).
- **G2 — site=DB 일치** ✅
  - USDM (BTC+ETH 1h): OI/globalLSR/basis/topLSR/taker **~50셀 소수점까지 완전 일치**.
  - COINM (BTCUSD_PERP 1h): OI(**contract 단위**)/globalLSR/basis **18셀** + SUIUSD globalLSR **6셀** 완전 일치.
  - taker 경계 봉 null = lookback 2봉 재수집이 해소(영구 누락 아님, `forwardFillTask.ts:174`).
- **G3 — transient/auth/quota 분리** ✅ (Step 0, d1~d5 PASS).
- **G4 — deferred 묘비 + 정정** ✅
  - `[8-26]`/`[8-3]`/`[8-20]` 회수(코드). `[8-31]` ⓐⓑⓒ 회수 + basis -1003 메커니즘 정정. `[8-33]` 금속 basis 회수.
  - 신규: `[8-34]` COINM 저유동 LSR sanity guard false positive. `[8-27]` 확장성 빚 가시화 유지(거래소 2개째 몫).
- **G5 — docs sync + complete** ✅ 본 파일.

---

## 3. 2026-06-06 최종 세션 — COINM 롤아웃 + 종단 검증

> 단일 진실 상세: `M1.9-step3-rollout.md §세션 재개 ~ §G`.

1. **라이브 재진단**: collector 23h+ 무중단(NRestarts=0, ActiveEnter 06-05 07:36). MainPID(부모 tsx 26653) ≠ 로그 PID(자식 node) = 정상 부모-자식 구조.
2. **G2 USDM 입증**: Binance 공식 `/futures/data` REST vs DB 동일 timestamp 대조 ~50셀 일치.
3. **basis -1003 규명** (§4): crypto-domain 자문 + 라이브 smoke.
4. **ⓑ 깔끔 종료 검증**: restart stop 2회 모두 `SIGTERM → graceful abort → Deactivated successfully` 1초 종료, SIGKILL 0. 인계 #1 해결.
5. **COINM 롤아웃**: `FORWARD_FILL_COINM=1` → `markets=[usdm,coinm] tasks=6`. (⚠️ 3중 인용 함정 2건 적발: `printf \n` 소실 → env 오염 → `sed`+`echo` 수정 / `grep "a|b"` 큰따옴표 소실.)
6. **G2 COINM 입증**: BTCUSD_PERP 18셀 + SUIUSD 6셀 site=DB 일치, OI contract 단위 검증.

---

## 4. basis `-1003` 근본 메커니즘 (확정, crypto-domain + 라이브)

- **fapi `/futures/data/basis` weight = 0** (공식 docs 2026-06-06, `X-MBX-USED-WEIGHT-1M` 헤더 미반환으로 라이브 입증). 과거 자문 "weight 1"은 dapi 기준 → fapi 정정.
- `-1003`의 `2400 req/min` = **REQUEST_WEIGHT 풀** (raw 요청 수 아님 — fapi exchangeInfo 에 RAW_REQUESTS 한도 부재).
- `10.119.x.x` = RFC1918 사설 IP = **Binance 내부 LB 노드** (우리 공인 IP 아님, 매 에러 가변).
- → **-1003 = 그 순간 우리를 받은 Binance LB 노드 weight 풀이 타 트래픽 합산으로 순간 포화. 우리 위반 아님(basis weight 0). 우리가 30/min 지켜도·줄여도 근절 불가 — graceful backoff 흡수가 정공.** 24h 1171회 = **무해 소음** (site=DB 일치가 증명, 최종 실패 ~1%·다음 lookback 채움).

---

## 5. 회수 deferred (묘비)

| ID | 내용 | 회수 Step |
|---|---|---|
| `[8-20]` | collector worker 인프라 추출 | Step 1 |
| `[8-26]` | history 정지(forward-fill) | Step 2 |
| `[8-3]` | COINM market_type 일반화 | Step 2 |
| `[8-31]` ⓐⓑⓒ | shared limiter + AbortSignal + per-metric throttle | Step 2~3 |
| `[8-33]` | 금속/주식 basis -4104 제외 | Step 3 |
| `[3-68]` | transient 진단 3분류 | Step 0 |

---

## 6. 잔여 (모니터링 / 후속, M1.9 차단 아님)

- **COINM 24~48h 장기 안정성** — ✅ **2026-06-07 PASS** (22h 시점 체크, 상세 `M1.9-coinm-stability.md`): collector `NRestarts=0` / 22h 무중단(ActiveEnter=COINM 롤아웃 시각 일치) / SIGKILL 0 / 6 task 전부 `success=true`. **same-IP ban 0** (우리 공인 IP 49.13.138.121 = 로그 0회, -1003 676회는 전부 내부 LB IP `10.119.x` 귀속 = §4 무해 소음 재현). DB: 20 심볼 × 9 interval 무구멍 누적(30h hourly gap 0) + 멱등 중복 0 + metric 채움률 ~100%. 메탈/주식 basis 제외(`[8-33]`)도 COINM 분기물에서 정상 작동 확인. (형식상 48h 시점 1분 재확인은 선택.)
- **`[8-34]` COINM 저유동 LSR sanity guard false positive** — ✅ **2026-06-07 회수** (위 모니터링 후속). 라이브 27h 로그의 ~40%(10,472줄)가 false positive 임을 실측 → `warnIfRatioOutOfRange` maxRatio 파라미터화(USDM 10 유지 / COINM 20). worker 134 test, code-reviewer 0 Critical. 상세 `M1.9-coinm-stability.md`.
- **`[8-31]` ⓓ circuit breaker / maxRetries 하향** (📋, 일반 복원력 — basis -1003 은 ⓓ로도 못 막음, Binance 측).
- **`[8-22]` warn 행 단위 폭발의 집계/샘플링** (🟡, `[8-34]` 가 임계값만 조정 — 구조적 집계는 미해결, 같은 함수 대상).
- **`[8-27]` 확장성 빚 6건** (거래소 2개째 진입 시).

---

## 7. 라이브 운영 상태 (2026-06-06 07:32 기준)

- **collector 서버**: 49.13.138.121 (Falkenstein, 별도 IP), `travis-collector-history.service`.
- **프로세스**: PID 26653 (commit 35ff502 = ⓐⓑⓒ 포함), `markets=[futures_usdm, futures_coinm] tasks=6`.
- **수집**: USDM 608 심볼(allowlist) + COINM **20 `_PERP` 심볼**(2026-06-07 라이브 DB 실측 — 전 심볼 9 interval 균일 누적; 롤아웃 시 ~17~18 추정은 첫 cycle 진행 중 스냅샷), 6 metric × 9 interval forward-fill.
- **production worker**(178.105.38.94, Nuremberg)와 **별도 IP** — `/futures/data` quota 격리 유지.

---

## 8. 비전공자 한 줄 요약

> **"두 번째 데이터 수집 직원(별도 서버)이 이제 USDM뿐 아니라 코인마진(COINM) 선물 데이터까지 24시간 창고에 쌓기 시작했고, 거래소 공식 사이트와 소수점까지 똑같다는 걸 확인했다. 수집 중 '너무 자주 온다'는 경고(-1003)가 계속 떴는데, 알고 보니 그건 우리 잘못이 아니라 거래소 쪽 창구가 전 세계 손님으로 붐벼서 나는 신호였고, 우리 직원은 잠깐 기다렸다 다시 물어보는 식(backoff)으로 데이터를 정확히 다 받아오고 있었다. M1.9 끝 — 이제 본인이 실제로 써보며 다음 메뉴를 정하는 단계로."**
