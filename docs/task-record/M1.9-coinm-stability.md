# M1.9 후속 — COINM 24~48h 안정성 체크 + `[8-34]` 회수 ✅

> **상태**: ✅ **완료 (2026-06-07)**. M1.9 ✅ 완료(`M1.9-complete.md`) 후속 모니터링 + deferred 1건 회수.
> **선행**: COINM 롤아웃 2026-06-06 07:32 UTC (`M1.9-complete.md §3 F`).
> **단일 진실 원천**: 본 파일.

---

## 0. 한 줄 요약

> **"COINM forward-fill 을 롤아웃 22h 시점에 점검 — collector 무중단(NRestarts=0) + same-IP ban 0 + DB 무구멍 누적으로 안정성 PASS. 점검 중 LSR sanity guard 가 전체 로그의 40%를 차지하는 false positive(`[8-34]`)임을 실측해 같은 세션에서 회수(maxRatio 파라미터화 USDM 10 / COINM 20)."**

---

## 1. 안정성 체크 결과 — ✅ PASS (2026-06-07 05:35 UTC, 롤아웃 +22h)

### 1.1 프로세스 무중단 (DB 가 증명 못 하는 핵심) — `systemctl show` + `journalctl`
| 항목 | 값 | 판정 |
|---|---|---|
| `NRestarts` | **0** | ✅ |
| `ActiveState / SubState` | active / running | ✅ |
| `ActiveEnterTimestamp` | **2026-06-06 07:32:52 UTC = COINM 롤아웃 시각 일치** | ✅ 22h 연속 |
| `SIGKILL` (27h) | **0** | ✅ ⓑ AbortSignal 정상 |
| Memory / CPU | 99MB(max 1G) / 22h간 11분 | ✅ |
| 6 task 실시간 status | usdm·coinm × short/mid/long 전부 `success=true, fails=0, lastErr=-` | ✅ |

> **방법론 교훈**: forward-fill 은 `getMaxRecordedAt` anchor 로 재시작 시 빈 구간을 자동 catch-up 하므로 **DB 의 "구멍 없음" 은 무중단과 중단후복구 둘 다와 양립**. 진짜 무중단은 `NRestarts`/uptime 으로만 확정 → SSH 로그 필수.

### 1.2 same-IP ban = 0 (사용자 핵심 요구사항) — ✅ 깨끗
- **우리 공인 IP `49.13.138.121` 로그 출현 = 0회.** 모든 `-1003`(676회/27h)은 내부 LB IP `10.119.17.162`(RFC1918) 귀속 + `(2xx rate-limit envelope) — backoff 재시도`. = `M1.9-complete.md §4` 메커니즘(Binance LB 노드 weight 풀 혼잡, basis weight 0, 우리 무관) 라이브 재현.
- `-1003` 빈도 06-06 1171회/24h → 06-07 ~676회/27h **감소**. 기능 영향 0 (basis 채움률 99.8~100%).
- ⚠️ `HTTP418=20` 은 전부 **substring 가짜 매치** (`basisRate=5.6418`, `418/608 symbol`, `rows=6,418`) — 실제 HTTP 418 IP-ban 상태코드 0건.

### 1.3 DB 지속 누적 (Supabase MCP) — ✅ 건강
- **20 심볼 × 9 interval 균일 누적** (각 7,173~7,178 rows). 롤아웃 18심볼 → 20심볼 전부 정상.
- **시간대 연속성**: 06-05 21:00 ~ 06-07 05:00 **gap 0** (매시 380~480 rows, 산수 일치: 20심볼 × 19봉/h = 380).
- **멱등 중복 0** (자연키 5축 onConflict).
- **metric 채움률(24h)**: OI/LSR 3종 100% · basis 99.8~100% · taker 98.7~100%(경계봉 confirm-lag = §B 기존 규명, 정상).
- **lag 정상**: 5m=5분 / 1h=1.6h / 12h·1d 큰 lag 는 "닫힌 봉만 수집" openTime 착시(§A).
- 보너스: COINM anchor 부재로 forward-fill 이 **05-23 까지 ~14일 과거 자동 backfill**(롤아웃 계획상 생략분이 공짜로 채워짐).

### 1.4 기타 (전부 무해 확인)
- `ERROR=2`: AAVEUSD/ADAUSD basis page Binance 일시 `400 data error` 2건/27h, graceful 재시도.
- `FAILED=262`: 대부분 `failed=0`·`실패 페이지=N`(USDM basis backoff ~1%)·status 라인. crash 0.

---

## 2. `[8-34]` 회수 — COINM LSR sanity guard false positive

### 2.1 문제 (라이브 실측)
- `LSR_WARN = 10,472줄 / 27h = 전체 로그 26,054줄의 ~40%`. `[normalizeCoinmGlobalLongShortHist] FILUSD longShortRatio=11.2399 (정상 0.1~10 범위 밖) — 데이터 이상 의심` 다발.
- 2026-06-06 dapi 라이브 대조(`M1.9-complete.md §3 G`)로 FILUSD/SUIUSD LSR 8~12 가 **실제 정상값**(site=DB 6/6 일치) 입증 → 상한 10(USDM 유동 기준)이 COINM 저유동(long 편향)에 false positive. **데이터는 warn-only 라 정확 저장**(폐기 X) = 순수 로그 위생/관측성 문제.

### 2.2 수정 (4 파일)
| 파일 | 변경 |
|---|---|
| `packages/exchange-collectors/.../normalize/_historyHelpers.ts` | `warnIfRatioOutOfRange` 에 `maxRatio = 10` 기본 파라미터 추가 + 메시지에 실제 상한 표시. **기본값 10 = USDM 3 호출부 미변경 = 회귀 0** (default-preserving extension). |
| `packages/exchange-collectors/.../normalize/coinmHistoryFutures.ts` | `COINM_MAX_LSR = 20` 상수 신설(관측 최대 ~12.5 + 헤드룸, dapi 입증 근거 주석) + COINM 3 호출부(account/position/global) 4번째 인자 전달. |
| `apps/worker/src/__tests__/coinmHistoryFutures.test.ts` | 회귀 테스트 4개: global(11.5→무경고 / 25→경고 / 0.05 하한→경고) + account(11.8→무경고, W3 호출부 가드). |

### 2.3 검증
- worker **134 test PASS** (130→134, +4, 회귀 0) · type-check 2패키지 Done · lint Done.
- **code-reviewer 0 Critical**. Warning 반영: W1(=`[8-22]` 교차 참조 — warn 폭발 집계 구조는 미해결) / W2(상한 20 은 관측 기반 추정, LSR>20 정상값 재출현 시 재조정) / W3(account 호출부 회귀 테스트 추가). S1(임계값 단일 정책 객체)=거래소 2개째 YAGNI 이월 / S2(하한 0.1 공통)=확인 / S3(한국어 console.warn=dev 로그 예외)=정합.

### 2.4 결정 근거
- **상한 20**: 관측 최대 ~12.5 의 1.6x 헤드룸 → 정상값 false positive 제거하되 진짜 이상치(>20)는 경고 유지. warn-only 라 초과해도 데이터 안전.
- **하한 0.1 공통 유지**: LSR<0.1(극단 short 쏠림)은 USDM·COINM 무관 드문 이상 신호 — 상한과 달리 구조적 비대칭 근거 없음.

---

## 3. 잔여 (차단 아님)
- 형식상 48h 시점(2026-06-08) 1분 재확인 — 선택 (22h PASS 추세 완벽).
- `[8-22]` warn 행 단위 폭발 집계/샘플링 (🟡, `[8-34]` 와 같은 함수, 구조적 미해결).
- `[8-31]`ⓓ circuit breaker (📋) / `[8-27]` 확장성 빚 (거래소 2개째).

---

## 4. 비전공자 한 줄 요약
> **"코인마진 데이터 수집 직원이 켠 지 22시간 동안 한 번도 안 쉬고(재시작 0회), 거래소한테 차단도 안 당하고, 창고(DB)에 빈칸 없이 정확히 쌓고 있음을 확인했다. 점검 중 '이 숫자 이상해요!' 경고가 전체 일지의 40%나 됐는데, 알고 보니 코인마진 틈새 종목은 원래 한쪽 쏠림이 심해 그게 정상값이었다. 그래서 경고 기준을 종목 특성에 맞게 (USDT선물 10 / 코인마진 20) 따로 조정해 가짜 경고를 없앴다. 데이터 자체는 원래도 정확했음."**
